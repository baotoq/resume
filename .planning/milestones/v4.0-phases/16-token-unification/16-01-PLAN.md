---
phase: 16-token-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Header.tsx
autonomous: true
requirements:
  - TOKEN-01
tags:
  - tokens
  - refactor
  - visual-parity
  - header
must_haves:
  truths:
    - "Header.tsx contains zero raw text-zinc-*, border-zinc-*, bg-zinc-*, text-indigo-*, outline-indigo-* classes"
    - "Header H1 uses text-foreground; subtitle paragraph uses text-foreground; bio paragraph uses text-muted-foreground"
    - "Middle-dot span between contact links uses text-muted-foreground"
    - "Contact <a> links use text-primary with hover:text-primary/80 and focus-visible:outline-primary"
    - "Inline · row structure (flex flex-wrap) and <address> wrapper unchanged"
  artifacts:
    - path: "src/components/Header.tsx"
      provides: "Color-token-driven header section"
      contains: "text-foreground"
  key_links: []
---

<objective>
Replace all raw zinc/indigo color classes in `src/components/Header.tsx` with shadcn semantic tokens per 16-CONTEXT.md D-01/D-02/D-03/D-05 and 16-UI-SPEC.md "Per-File Contracts — File 1". No JSX changes, no layout changes, no new imports. Five class substitutions on five lines.

Purpose: Eliminate TOKEN-01 violations in Header component. Contribute to phase-wide monochrome token direction (CONTEXT D-05).
Output: `src/components/Header.tsx` with zero raw color classes, biome lint clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-token-unification/16-CONTEXT.md
@.planning/phases/16-token-unification/16-UI-SPEC.md
@.planning/phases/16-token-unification/16-RESEARCH.md
@src/components/Header.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap raw color classes in Header.tsx to shadcn tokens (TOKEN-01)</name>
  <files>src/components/Header.tsx</files>
  <read_first>
    - src/components/Header.tsx (confirm current state — 5 lines to modify: 31, 34, 40, 43, 54)
    - src/app/globals.css (confirm :root tokens foreground, muted-foreground, primary exist)
    - .planning/phases/16-token-unification/16-UI-SPEC.md (File 1 contract table)
    - .planning/phases/16-token-unification/16-CONTEXT.md (D-01, D-02, D-03, D-05)
  </read_first>
  <action>
Apply these exact substitutions to `src/components/Header.tsx`. Only the color-related class tokens change — all other classes on the same attribute are preserved verbatim.

**Line 31** — H1 name heading:
BEFORE:
```tsx
<h1 className="text-[28px] font-semibold leading-[1.1] text-zinc-900">
```
AFTER:
```tsx
<h1 className="text-[28px] font-semibold leading-[1.1] text-foreground">
```

**Line 34** — subtitle paragraph:
BEFORE:
```tsx
<p className="text-xl font-semibold leading-[1.2] text-zinc-700 mt-1">
```
AFTER:
```tsx
<p className="text-xl font-semibold leading-[1.2] text-foreground mt-1">
```

**Line 40** — middle-dot separator span between contact links:
BEFORE:
```tsx
{i > 0 && <span className="text-zinc-400"> · </span>}
```
AFTER:
```tsx
{i > 0 && <span className="text-muted-foreground"> · </span>}
```

**Line 43** — contact link `<a>`:
BEFORE:
```tsx
className="text-indigo-600 hover:text-indigo-700 hover:underline focus-visible:outline focus-visible:outline-2 focus-visible:outline-indigo-600 focus-visible:outline-offset-2"
```
AFTER:
```tsx
className="text-primary hover:text-primary/80 hover:underline focus-visible:outline focus-visible:outline-2 focus-visible:outline-primary focus-visible:outline-offset-2"
```
(Three token swaps on the same attribute: `text-indigo-600` → `text-primary`; `hover:text-indigo-700` → `hover:text-primary/80`; `focus-visible:outline-indigo-600` → `focus-visible:outline-primary`.)

**Line 54** — bio paragraph:
BEFORE:
```tsx
<p className="mt-4 text-base leading-relaxed text-zinc-600">
```
AFTER:
```tsx
<p className="mt-4 text-base leading-relaxed text-muted-foreground">
```

**Constraints (per locked decisions):**
- Do NOT add any imports.
- Do NOT change JSX structure: H1, `<p>`, `<address>`, `<span>`, `<a>`, `<ul>`, `<li>` all preserved.
- Do NOT touch `flex flex-wrap` contact row layout (Phase 15 D-07 lock).
- Do NOT touch non-color utilities on the same attributes: `text-[28px]`, `font-semibold`, `leading-[1.1]`, `text-xl`, `leading-[1.2]`, `mt-1`, `mt-4`, `text-base`, `leading-relaxed`, `hover:underline`, `focus-visible:outline`, `focus-visible:outline-2`, `focus-visible:outline-offset-2` — all preserved verbatim.
- Do NOT add a `className` prop anywhere new.
- Do NOT introduce `cn()` helper — raw class strings only, matching existing file style.

After edits, run formatter (no new imports but keeps whitespace consistent):
```bash
npm run format
```
  </action>
  <verify>
    <automated>test $(rg -c "text-zinc-|border-zinc-|bg-zinc-|text-indigo-|bg-indigo-|outline-indigo-|bg-blue-|text-blue-|bg-white|text-white" src/components/Header.tsx || echo 0) -eq 0 && test $(grep -c "text-foreground" src/components/Header.tsx) -ge 2 && test $(grep -c "text-muted-foreground" src/components/Header.tsx) -ge 2 && test $(grep -c "text-primary" src/components/Header.tsx) -ge 1 && test $(grep -c "outline-primary" src/components/Header.tsx) -eq 1 && test $(grep -c "hover:text-primary/80" src/components/Header.tsx) -eq 1 && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "text-zinc-|border-zinc-|bg-zinc-|text-indigo-|bg-indigo-|outline-indigo-|bg-blue-|text-blue-|bg-white|text-white" src/components/Header.tsx` returns 0 (or `rg` exits 1 = no matches)
    - `grep -c "text-foreground" src/components/Header.tsx` returns `>= 2` (H1 + subtitle `<p>`)
    - `grep -c "text-muted-foreground" src/components/Header.tsx` returns `>= 2` (middle-dot span + bio `<p>`)
    - `grep -c "text-primary" src/components/Header.tsx` returns `>= 1` (contact `<a>`)
    - `grep -c "hover:text-primary/80" src/components/Header.tsx` returns `1`
    - `grep -c "outline-primary" src/components/Header.tsx` returns `1`
    - `grep -c "flex flex-wrap" src/components/Header.tsx` returns `>= 1` (inline contact row layout preserved — Phase 15 D-07 lock)
    - `grep -c "<address" src/components/Header.tsx` returns `>= 1` (address landmark preserved)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>
    Header.tsx uses only shadcn semantic tokens for color. Zero raw zinc/indigo classes. Contact-link accent now near-black via `--primary`. Layout, typography, JSX structure unchanged. Biome clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. No input surfaces. No data flow changes. Pure class-string refactor of a Server Component that reads `resume.md` YAML.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | Header color classes | mitigate | Ripgrep acceptance criterion enforces zero raw color classes. Class-level unit enforcement. |
| T-16-02 | Tampering | Header layout invariants | mitigate | Acceptance criteria `flex flex-wrap >= 1` and `<address >= 1` guard against accidental structural changes during class swap. |
</threat_model>

<verification>
- `rg "text-zinc-|border-zinc-|bg-zinc-|text-indigo-|bg-indigo-|outline-indigo-|bg-blue-|text-blue-|bg-white|text-white" src/components/Header.tsx` → zero matches
- `grep -c "text-foreground" src/components/Header.tsx` → ≥ 2
- `grep -c "text-muted-foreground" src/components/Header.tsx` → ≥ 2
- `grep -c "text-primary" src/components/Header.tsx` → ≥ 1
- `grep -c "hover:text-primary/80" src/components/Header.tsx` → 1
- `grep -c "outline-primary" src/components/Header.tsx` → 1
- `npm run lint` → exit 0
- (Plan 16-05 phase gate runs build + whole-directory grep + visual parity check)
</verification>

<success_criteria>
- TOKEN-01 partial (Header scope) satisfied.
- Phase SC#1 partial (Header scope) satisfied.
- Indigo/zinc absent from Header.tsx.
- Biome lint passes.
- Layout/typography invariant preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/16-token-unification/16-01-SUMMARY.md` documenting: lines changed, tokens applied, any surprises, verification output.
</output>
