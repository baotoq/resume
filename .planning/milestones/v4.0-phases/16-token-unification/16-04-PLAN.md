---
phase: 16-token-unification
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/techstack-icons/TechStackIcons.tsx
autonomous: true
requirements:
  - TOKEN-01
tags:
  - tokens
  - refactor
  - visual-parity
  - tooltip
must_haves:
  truths:
    - "TechStackIcons.tsx tooltip span uses bg-popover text-popover-foreground border border-border"
    - "TechStackIcons.tsx contains zero raw bg-zinc-* or text-white classes"
    - "Icon-hit branch JSX structure (relative group wrapper, Icon size=40, absolute positioning utilities on tooltip) preserved"
    - "Fallback branch using Badge variant=secondary (from Phase 15) is unchanged"
  artifacts:
    - path: "src/components/techstack-icons/TechStackIcons.tsx"
      provides: "Token-driven tech-stack tooltip"
      contains: "bg-popover text-popover-foreground"
  key_links: []
---

<objective>
Replace the tooltip-pill colors in `src/components/techstack-icons/TechStackIcons.tsx` (line 82) per 16-CONTEXT.md D-07 and 16-UI-SPEC.md "Per-File Contracts — File 4". Two token swaps plus one additive `border border-border` class on the tooltip `<span>`. No other changes.

Purpose: Eliminate the last `bg-zinc-*` + `text-white` occurrence in the four in-scope components. Bring the tooltip surface onto the shadcn popover token.
Output: `src/components/techstack-icons/TechStackIcons.tsx` with the tooltip on `bg-popover text-popover-foreground border border-border` and zero raw color classes across the file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-token-unification/16-CONTEXT.md
@.planning/phases/16-token-unification/16-UI-SPEC.md
@.planning/phases/16-token-unification/16-RESEARCH.md
@src/components/techstack-icons/TechStackIcons.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap tooltip color classes to shadcn popover tokens (TOKEN-01)</name>
  <files>src/components/techstack-icons/TechStackIcons.tsx</files>
  <read_first>
    - src/components/techstack-icons/TechStackIcons.tsx (confirm current state — tooltip span at line 82; fallback branch at ~line 89 using Badge — do NOT touch)
    - src/app/globals.css (confirm :root tokens popover, popover-foreground, border exist)
    - .planning/phases/16-token-unification/16-UI-SPEC.md (File 4 contract)
    - .planning/phases/16-token-unification/16-CONTEXT.md (D-07)
    - .planning/phases/15-badge-and-separator/15-CONTEXT.md (D-08 — icon branch lock from Phase 15 still applies: fallback Badge unchanged)
  </read_first>
  <action>
Apply this exact substitution to line 82 tooltip `<span>` inside the icon-hit branch of `TechIcon`:

BEFORE:
```tsx
<span className="absolute -top-8 left-1/2 -translate-x-1/2 bg-zinc-800 text-white text-xs px-2 py-1 rounded opacity-0 group-hover:opacity-100 transition-opacity duration-150 whitespace-nowrap pointer-events-none z-10">
```

AFTER:
```tsx
<span className="absolute -top-8 left-1/2 -translate-x-1/2 bg-popover text-popover-foreground border border-border text-xs px-2 py-1 rounded opacity-0 group-hover:opacity-100 transition-opacity duration-150 whitespace-nowrap pointer-events-none z-10">
```

Exact diff (three class changes, in order of appearance):
- `bg-zinc-800` → `bg-popover`
- `text-white` → `text-popover-foreground`
- Insert `border border-border` immediately after `text-popover-foreground` and before `text-xs` (additive — this is the only new utility in the phase).

**Constraints:**
- Do NOT modify the fallback branch (`<Badge variant="secondary">{tech}</Badge>`) — Phase 15 locked it.
- Do NOT modify `TECH_ICON_MAP`, `normalizeTech`, `TechStackIconsProps`, `IconComponent`, or the `TechStackIcons` wrapper.
- Do NOT modify positioning/animation utilities: `absolute -top-8 left-1/2 -translate-x-1/2`, `text-xs px-2 py-1`, `rounded`, `opacity-0 group-hover:opacity-100 transition-opacity duration-150`, `whitespace-nowrap pointer-events-none z-10`.
- Do NOT add a `className` prop anywhere new.
- Do NOT add imports.

After edits:
```bash
npm run format
```
  </action>
  <verify>
    <automated>test $(rg -c "text-zinc-|border-zinc-|bg-zinc-|text-indigo-|bg-indigo-|outline-indigo-|bg-blue-|text-blue-|bg-white|text-white" src/components/techstack-icons/TechStackIcons.tsx || echo 0) -eq 0 && test $(grep -c "bg-popover" src/components/techstack-icons/TechStackIcons.tsx) -eq 1 && test $(grep -c "text-popover-foreground" src/components/techstack-icons/TechStackIcons.tsx) -eq 1 && test $(grep -c "border border-border" src/components/techstack-icons/TechStackIcons.tsx) -eq 1 && test $(grep -c "Badge variant=\"secondary\"" src/components/techstack-icons/TechStackIcons.tsx) -ge 1 && test $(grep -c "TECH_ICON_MAP" src/components/techstack-icons/TechStackIcons.tsx) -ge 2 && test $(grep -c "relative group" src/components/techstack-icons/TechStackIcons.tsx) -ge 1 && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "text-zinc-|border-zinc-|bg-zinc-|text-indigo-|bg-indigo-|outline-indigo-|bg-blue-|text-blue-|bg-white|text-white" src/components/techstack-icons/TechStackIcons.tsx` returns 0 / no matches
    - `grep -c "bg-popover" src/components/techstack-icons/TechStackIcons.tsx` returns `1`
    - `grep -c "text-popover-foreground" src/components/techstack-icons/TechStackIcons.tsx` returns `1`
    - `grep -c "border border-border" src/components/techstack-icons/TechStackIcons.tsx` returns `1`
    - `grep -c "Badge variant=\"secondary\"" src/components/techstack-icons/TechStackIcons.tsx` returns `>= 1` (Phase 15 fallback preserved)
    - `grep -c "TECH_ICON_MAP" src/components/techstack-icons/TechStackIcons.tsx` returns `>= 2` (map definition + lookup preserved)
    - `grep -c "relative group" src/components/techstack-icons/TechStackIcons.tsx` returns `>= 1` (icon-hit branch wrapper preserved — Phase 15 SC#2 lock)
    - `grep -c "group-hover:opacity-100" src/components/techstack-icons/TechStackIcons.tsx` returns `1` (tooltip hover animation preserved)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>
    Tooltip pill now uses `bg-popover text-popover-foreground border border-border`. Fallback Badge branch (Phase 15) untouched. Icon-hit branch JSX shape unchanged. Biome clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. Client-side DOM only. `tech` string still React-escaped as children.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | Tampering | Tooltip color classes | mitigate | Ripgrep acceptance criterion enforces no raw colors remain. Count criteria (`bg-popover = 1`, `border border-border = 1`) guard against over-application. |
| T-16-07 | Tampering | Phase 15 fallback Badge branch | mitigate | Acceptance criteria `Badge variant="secondary" >= 1` and `TECH_ICON_MAP >= 2` and `relative group >= 1` guard the icon-hit + fallback structure from accidental modification. |
</threat_model>

<verification>
- `rg "text-zinc-|border-zinc-|bg-zinc-|text-indigo-|bg-indigo-|outline-indigo-|bg-blue-|text-blue-|bg-white|text-white" src/components/techstack-icons/TechStackIcons.tsx` → zero matches
- Token/lock greps as above
- `npm run lint` → exit 0
- (Plan 16-05 phase gate runs build + whole-directory grep + visual parity check, includes tooltip hover at 375px + 1280px)
</verification>

<success_criteria>
- TOKEN-01 partial (TechStackIcons tooltip scope) satisfied.
- Phase SC#1 partial (TechStackIcons scope) satisfied.
- Phase 15 fallback Badge preserved.
- Biome lint passes.
</success_criteria>

<output>
After completion, create `.planning/phases/16-token-unification/16-04-SUMMARY.md` documenting: line changed, tokens applied, rationale for `border border-border` additive class, verification output.
</output>
