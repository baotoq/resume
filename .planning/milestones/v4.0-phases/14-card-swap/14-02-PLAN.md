---
phase: 14-card-swap
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/WorkExperience.tsx
autonomous: true
requirements:
  - CARD-02
tags:
  - shadcn
  - card
  - refactor
  - visual-parity
must_haves:
  truths:
    - "Each WorkExperience entry renders inside shadcn <Card><CardContent>"
    - "Outer <article> landmark preserved per entry (a11y)"
    - "Timeline dot (lines 46-53) remains sibling of <article> — not moved inside Card (D-06)"
    - "Inner `flex flex-col gap-4` wrapper preserved — governs header-row → bullets → tech-icons spacing (Finding #2)"
    - "Hand-rolled card utility string is gone from WorkExperience.tsx"
  artifacts:
    - path: "src/components/WorkExperience.tsx"
      provides: "Timeline entries each wrapped in <article><Card><CardContent>"
      contains: "import { Card, CardContent } from \"@/components/ui/card\""
  key_links:
    - from: "src/components/WorkExperience.tsx"
      to: "src/components/ui/card.tsx"
      via: "named import Card, CardContent"
      pattern: "from \"@/components/ui/card\""
---

<objective>
Swap the hand-rolled card wrapper in `src/components/WorkExperience.tsx` (line 56) for the shadcn `<Card><CardContent>` primitive. Satisfies CARD-02. Per D-01 the per-entry `<article>` landmark stays; per D-06 the timeline dot stays as a sibling; per Finding #2 the inner `flex flex-col gap-4` wrapper is KEPT (Card's `gap-6` does not apply to grandchildren).

Purpose: Adopt shadcn Card for timeline entries while preserving the timeline dot and internal vertical rhythm.
Output: `src/components/WorkExperience.tsx` using `<article><Card><CardContent>` per entry, timeline dot untouched, no className overrides on Card.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-card-swap/14-CONTEXT.md
@.planning/phases/14-card-swap/14-UI-SPEC.md
@.planning/phases/14-card-swap/14-RESEARCH.md
@src/components/WorkExperience.tsx
@src/components/ui/card.tsx

<interfaces>
<!-- From src/components/ui/card.tsx — same as plan 14-01 -->
```tsx
function Card({ className, ...props }: React.ComponentProps<"div">)
// "flex flex-col gap-6 rounded-xl border bg-card py-6 text-card-foreground shadow-sm"

function CardContent({ className, ...props }: React.ComponentProps<"div">)
// "px-6"
```
RSC-safe.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap WorkExperience.tsx per-entry card to shadcn Card primitive (CARD-02)</name>
  <files>src/components/WorkExperience.tsx</files>
  <read_first>
    - src/components/WorkExperience.tsx (the file being modified)
    - src/components/ui/card.tsx (primitive source — confirm default classes)
    - .planning/phases/14-card-swap/14-UI-SPEC.md (Per-File JSX Contract for WorkExperience.tsx)
    - .planning/phases/14-card-swap/14-CONTEXT.md (D-01 landmark, D-06 timeline dot, D-07 scope lock)
    - .planning/phases/14-card-swap/14-RESEARCH.md Finding #2 (why inner `gap-4` wrapper stays)
  </read_first>
  <action>
Implement per D-01..D-07 and 14-UI-SPEC.md "Per-File JSX Contract — WorkExperience.tsx".

**Step 1 — Add import** after the existing imports (the last import block ends at `computeDuration`):

```tsx
import { Card, CardContent } from "@/components/ui/card";
```

**Step 2 — Replace the opening `<article>` at line 56:**

BEFORE (line 56):
```tsx
              <article className="rounded-xl border border-zinc-200 bg-white p-6 shadow-sm">
                <div className="flex flex-col gap-4">
```

AFTER (line 56 expands to four lines; the `flex flex-col gap-4` inner wrapper is KEPT, do NOT remove it — per Finding #2):
```tsx
              <article>
                <Card>
                  <CardContent>
                    <div className="flex flex-col gap-4">
```

**Step 3 — Replace the closing `</article>` at line 94:**

BEFORE (line 93-94):
```tsx
                </div>
              </article>
```

AFTER (the closing `</div>` for `flex flex-col gap-4` stays, then add CardContent/Card closers):
```tsx
                  </div>
                </CardContent>
              </Card>
            </article>
```

**Constraints (per locked decisions):**
- Do NOT touch the timeline dot at lines 46-53 (the `<div className={'absolute z-10 -left-5.5 ...'}>`). It remains a sibling of `<article>` inside the parent `<div key={index} className="relative">`. (D-06)
- Do NOT remove the inner `<div className="flex flex-col gap-4">` wrapper — it is the sole source of spacing between the header row, bullets `<ul>`, and `<TechStackIcons />`. Card's `gap-6` does not reach grandchildren. (Finding #2)
- Do NOT add any `className` prop to `<Card>` or `<CardContent>` (D-03/04/05).
- Do NOT introduce `<CardHeader>`, `<CardTitle>`, `<CardDescription>`, or `<CardFooter>` (D-02).
- Do NOT modify the inner header row, `<ul>` bullets, or `<TechStackIcons />`.
- Indentation: shift the inner block one additional level (two extra spaces) so Biome stays clean. Run `npm run format` after if needed.
  </action>
  <verify>
    <automated>test $(grep -c 'rounded-xl border border-zinc-200 bg-white p-6 shadow-sm' src/components/WorkExperience.tsx) -eq 0 && test $(grep -c '<Card>' src/components/WorkExperience.tsx) -ge 1 && test $(grep -c '<CardContent>' src/components/WorkExperience.tsx) -ge 1 && test $(grep -c 'from "@/components/ui/card"' src/components/WorkExperience.tsx) -eq 1 && test $(grep -c '<article>' src/components/WorkExperience.tsx) -ge 1 && test $(grep -c 'flex flex-col gap-4' src/components/WorkExperience.tsx) -ge 1 && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rounded-xl border border-zinc-200 bg-white p-6 shadow-sm" src/components/WorkExperience.tsx` returns `0`
    - `grep -c "<Card>" src/components/WorkExperience.tsx` returns `>= 1`
    - `grep -c "<CardContent>" src/components/WorkExperience.tsx` returns `>= 1`
    - `grep -c 'from "@/components/ui/card"' src/components/WorkExperience.tsx` returns `1`
    - `grep -c "<article>" src/components/WorkExperience.tsx` returns `>= 1` (landmark preserved, D-01)
    - `grep -c "flex flex-col gap-4" src/components/WorkExperience.tsx` returns `>= 1` (inner wrapper preserved, Finding #2)
    - `grep -c "absolute z-10 -left-5.5" src/components/WorkExperience.tsx` returns `>= 1` (timeline dot preserved, D-06)
    - `grep -E 'Card className=|CardContent className=' src/components/WorkExperience.tsx` returns no matches (no overrides)
    - `grep -E 'CardHeader|CardTitle|CardDescription|CardFooter' src/components/WorkExperience.tsx` returns no matches (D-02)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>
    Every WorkExperience entry renders as `<article><Card><CardContent><div className="flex flex-col gap-4">...</div></CardContent></Card></article>` with the timeline dot sibling preserved. Lint clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. JSX-only refactor; existing data flow (YAML → props → JSX) unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Tampering | WorkExperience.tsx `<article>` landmark | mitigate | Acceptance criterion `grep -c "<article>" >= 1` enforces landmark preservation (D-01). |
| T-14-05 | Tampering | Internal vertical rhythm (`flex flex-col gap-4`) | mitigate | Acceptance criterion `grep -c "flex flex-col gap-4" >= 1` prevents accidental removal (Finding #2 / Pitfall #1). |
| T-14-06 | Tampering | Timeline dot | mitigate | Acceptance criterion `grep -c "absolute z-10 -left-5.5" >= 1` enforces D-06. |
| T-14-07 | Information Disclosure | Card children (company/role/bullets) | accept | React default escaping; no new data path; HighlightedBullet left untouched. |
</threat_model>

<verification>
- Hand-rolled pattern gone: `grep -c "rounded-xl border border-zinc-200 bg-white p-6 shadow-sm" src/components/WorkExperience.tsx` → 0
- Card primitive used: `grep -c "<Card>" src/components/WorkExperience.tsx` → >= 1
- Landmark preserved: `grep -c "<article>" src/components/WorkExperience.tsx` → >= 1
- Inner wrapper preserved: `grep -c "flex flex-col gap-4" src/components/WorkExperience.tsx` → >= 1
- Timeline dot preserved: `grep -c "absolute z-10 -left-5.5" src/components/WorkExperience.tsx` → >= 1
- Lint clean: `npm run lint` → exit 0
</verification>

<success_criteria>
- CARD-02 satisfied: Each WorkExperience entry uses shadcn Card, hand-rolled classes removed.
- `<article>` landmarks preserved (D-01).
- Timeline dot sibling preserved (D-06).
- Inner `flex flex-col gap-4` wrapper preserved (Finding #2).
- No Card/CardContent className overrides.
- Biome lint passes.
</success_criteria>

<output>
After completion, create `.planning/phases/14-card-swap/14-02-SUMMARY.md` documenting:
- Diff applied (before/after snippets)
- Grep verification results
- Lint result
- Confirmation that the inner `flex flex-col gap-4` wrapper was KEPT (not removed) per Finding #2
</output>
