---
phase: 14-card-swap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Header.tsx
autonomous: true
requirements:
  - CARD-01
tags:
  - shadcn
  - card
  - refactor
  - visual-parity
must_haves:
  truths:
    - "Header renders with shadcn Card primitive wrapping existing content"
    - "Outer <section> landmark preserved (a11y)"
    - "Visual result at 375px and 1280px is parity with pre-swap (accepting D-05 14px radius)"
    - "Hand-rolled card utility string is gone from Header.tsx"
  artifacts:
    - path: "src/components/Header.tsx"
      provides: "Header component using <Card><CardContent>"
      contains: "import { Card, CardContent } from \"@/components/ui/card\""
  key_links:
    - from: "src/components/Header.tsx"
      to: "src/components/ui/card.tsx"
      via: "named import Card, CardContent"
      pattern: "from \"@/components/ui/card\""
---

<objective>
Swap the hand-rolled card wrapper in `src/components/Header.tsx` (line 17) for the shadcn `<Card><CardContent>` primitive installed in Phase 13. Satisfies CARD-01. Per D-01 the outer `<section>` landmark stays; per D-02 only Card + CardContent are used; per D-03/04/05 no className overrides on Card.

Purpose: Adopt the shadcn design system token surface for the Header card — single source of truth for card styling.
Output: `src/components/Header.tsx` using `<Card><CardContent>` with all children intact, zero utility-class overrides on the Card.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-card-swap/14-CONTEXT.md
@.planning/phases/14-card-swap/14-UI-SPEC.md
@.planning/phases/14-card-swap/14-RESEARCH.md
@src/components/Header.tsx
@src/components/ui/card.tsx

<interfaces>
<!-- From src/components/ui/card.tsx — extracted exports used by this plan -->
```tsx
function Card({ className, ...props }: React.ComponentProps<"div">)
// default classes: "flex flex-col gap-6 rounded-xl border bg-card py-6 text-card-foreground shadow-sm"

function CardContent({ className, ...props }: React.ComponentProps<"div">)
// default classes: "px-6"

export { Card, CardContent, ... }
```
Both are Server Component safe (no `"use client"` directive in the file).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap Header.tsx to shadcn Card primitive (CARD-01)</name>
  <files>src/components/Header.tsx</files>
  <read_first>
    - src/components/Header.tsx (the file being modified)
    - src/components/ui/card.tsx (primitive source — confirm default classes and RSC-safety)
    - .planning/phases/14-card-swap/14-UI-SPEC.md (Per-File JSX Contract for Header.tsx)
    - .planning/phases/14-card-swap/14-CONTEXT.md (D-01 through D-08 locked decisions)
  </read_first>
  <action>
Implement per D-01..D-05 and 14-UI-SPEC.md "Per-File JSX Contract — Header.tsx".

**Step 1 — Add import** at the top of the file, below the existing `import type { ResumeData } from "@/types/resume"`:

```tsx
import { Card, CardContent } from "@/components/ui/card"
```

**Step 2 — Replace the opening `<section>` at line 17:**

BEFORE (line 17):
```tsx
<section className="rounded-xl border border-zinc-200 bg-white px-6 py-6 shadow-sm">
```

AFTER (line 17 becomes three lines):
```tsx
<section>
  <Card>
    <CardContent>
```

**Step 3 — Replace the closing `</section>` at line 37:**

BEFORE (line 37):
```tsx
</section>
```

AFTER (line 37 becomes three lines):
```tsx
    </CardContent>
  </Card>
</section>
```

**Constraints (per locked decisions):**
- Do NOT add any `className` prop to `<Card>` or `<CardContent>` (per D-03/04/05 — defaults are correct).
- Do NOT add `bg-white`, `border-zinc-200`, or `rounded-*` overrides (per D-04/05).
- Do NOT touch any of the children: `<h1>`, `<p>`, the contact `<div>`, the bio `<p>`. All children retain their exact classes and text (per D-02 and Preservation Contract in 14-UI-SPEC.md).
- Do NOT introduce `<CardHeader>`, `<CardTitle>`, `<CardDescription>`, or `<CardFooter>` (per D-02).
- Indentation: shift all child lines inside `<CardContent>` one additional level (4 spaces) so Biome formatting stays clean. Run `npm run format` after the edit if needed.

The outer `<section>` now carries zero classes — it is a pure semantic landmark wrapper.
  </action>
  <verify>
    <automated>test $(grep -c 'rounded-xl border border-zinc-200 bg-white' src/components/Header.tsx) -eq 0 && test $(grep -c '<Card>' src/components/Header.tsx) -ge 1 && test $(grep -c '<CardContent>' src/components/Header.tsx) -ge 1 && test $(grep -c 'from "@/components/ui/card"' src/components/Header.tsx) -eq 1 && test $(grep -c '<section>' src/components/Header.tsx) -ge 1 && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rounded-xl border border-zinc-200 bg-white" src/components/Header.tsx` returns `0` (hand-rolled pattern gone)
    - `grep -c "<Card>" src/components/Header.tsx` returns `>= 1`
    - `grep -c "<CardContent>" src/components/Header.tsx` returns `>= 1`
    - `grep -c 'from "@/components/ui/card"' src/components/Header.tsx` returns `1` (single import line)
    - `grep -c "<section>" src/components/Header.tsx` returns `>= 1` (landmark preserved, D-01)
    - `grep -E 'Card className=|CardContent className=' src/components/Header.tsx` returns no matches (no overrides, D-03/04/05)
    - `grep -E 'CardHeader|CardTitle|CardDescription|CardFooter' src/components/Header.tsx` returns no matches (D-02)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>
    Header.tsx renders Header through `<section><Card><CardContent>...</CardContent></Card></section>`. Import present. No className props on Card/CardContent. Lint clean. All other children preserved verbatim.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. No user input surfaces are added; this is a visual-only JSX refactor. Existing boundary (YAML frontmatter → gray-matter → props) is unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | src/components/Header.tsx landmark structure | mitigate | Acceptance criterion `grep -c "<section>" >= 1` enforces landmark preservation; D-01 bans dropping `<section>`. |
| T-14-02 | Tampering | src/components/Header.tsx color tokens | mitigate | Acceptance criterion `grep -E 'Card className=' returns no matches` prevents `bg-white`/`border-zinc-200` reintroduction (D-04). |
| T-14-03 | Information Disclosure | Card children (resume name/title/bio) | accept | React default escaping applies; existing strings are YAML-authored and were already rendered pre-swap. No new data path. |
</threat_model>

<verification>
- `grep -c "rounded-xl border border-zinc-200 bg-white" src/components/Header.tsx` → 0
- `grep -c "<Card>" src/components/Header.tsx` → >= 1
- `grep -c "<CardContent>" src/components/Header.tsx` → >= 1
- `grep -c "<section>" src/components/Header.tsx` → >= 1 (landmark preservation, D-01)
- `grep -c 'from "@/components/ui/card"' src/components/Header.tsx` → 1
- `npm run lint` → exit 0
- (Plan 14-04 gate runs full build + visual parity check)
</verification>

<success_criteria>
- CARD-01 satisfied: Header uses shadcn Card primitive, hand-rolled classes removed.
- `<section>` landmark preserved (D-01).
- No className overrides on Card/CardContent (D-03/04/05).
- No CardHeader/CardTitle/etc. introduced (D-02).
- Biome lint passes.
</success_criteria>

<output>
After completion, create `.planning/phases/14-card-swap/14-01-SUMMARY.md` documenting:
- The exact diff applied
- Grep verification output (zeros and positives)
- Lint result
- Any decisions exercised under "Claude's Discretion" (default: none — no overrides)
</output>
