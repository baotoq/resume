---
phase: 14-card-swap
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/EducationSection.tsx
autonomous: true
requirements:
  - CARD-03
tags:
  - shadcn
  - card
  - refactor
  - visual-parity
must_haves:
  truths:
    - "Each EducationSection entry renders inside shadcn <Card><CardContent>"
    - "Outer <article> landmark preserved with key={index} on <article>, not on <Card> (Pitfall #3)"
    - "Details paragraph `mt-4` preserved verbatim (not affected by Card's gap-6 since it is not a direct Card child)"
    - "Hand-rolled card utility string is gone from EducationSection.tsx"
  artifacts:
    - path: "src/components/EducationSection.tsx"
      provides: "Education entries each wrapped in <article key={index}><Card><CardContent>"
      contains: "import { Card, CardContent } from \"@/components/ui/card\""
  key_links:
    - from: "src/components/EducationSection.tsx"
      to: "src/components/ui/card.tsx"
      via: "named import Card, CardContent"
      pattern: "from \"@/components/ui/card\""
---

<objective>
Swap the hand-rolled card wrapper in `src/components/EducationSection.tsx` (lines 30-33) for the shadcn `<Card><CardContent>` primitive. Satisfies CARD-03. Per D-01 the per-entry `<article>` landmark stays (with `key={index}` on it, not on Card — Pitfall #3).

Purpose: Adopt shadcn Card for education entries; preserve list reconciliation key on the semantic landmark.
Output: `src/components/EducationSection.tsx` using `<article key={index}><Card><CardContent>` per entry, no className overrides on Card.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-card-swap/14-CONTEXT.md
@.planning/phases/14-card-swap/14-UI-SPEC.md
@.planning/phases/14-card-swap/14-RESEARCH.md
@src/components/EducationSection.tsx
@src/components/ui/card.tsx

<interfaces>
<!-- From src/components/ui/card.tsx — same as plans 14-01/02 -->
```tsx
function Card({ className, ...props }: React.ComponentProps<"div">)
// "flex flex-col gap-6 rounded-xl border bg-card py-6 text-card-foreground shadow-sm"

function CardContent({ className, ...props }: React.ComponentProps<"div">)
// "px-6"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap EducationSection.tsx per-entry card to shadcn Card primitive (CARD-03)</name>
  <files>src/components/EducationSection.tsx</files>
  <read_first>
    - src/components/EducationSection.tsx (the file being modified)
    - src/components/ui/card.tsx (primitive source — confirm default classes)
    - .planning/phases/14-card-swap/14-UI-SPEC.md (Per-File JSX Contract for EducationSection.tsx)
    - .planning/phases/14-card-swap/14-CONTEXT.md (D-01..D-08)
    - .planning/phases/14-card-swap/14-RESEARCH.md Pitfall #3 (key prop placement)
  </read_first>
  <action>
Implement per D-01..D-05, D-07 and 14-UI-SPEC.md "Per-File JSX Contract — EducationSection.tsx".

**Step 1 — Add import** below the existing `import type { EducationEntry }`:

```tsx
import { Card, CardContent } from "@/components/ui/card";
```

**Step 2 — Replace the opening `<article>` block at lines 30-33:**

BEFORE (lines 30-33):
```tsx
          <article
            key={index}
            className="rounded-xl border border-zinc-200 bg-white p-6 shadow-sm"
          >
```

AFTER (collapse to `<article key={index}>` and add Card/CardContent inside — the `key` prop stays on `<article>`, NOT on `<Card>`, per Pitfall #3):
```tsx
          <article key={index}>
            <Card>
              <CardContent>
```

**Step 3 — Replace the closing `</article>` at line 50:**

BEFORE (line 50):
```tsx
          </article>
```

AFTER (add CardContent/Card closers inside, `</article>` stays outermost):
```tsx
              </CardContent>
            </Card>
          </article>
```

**Constraints (per locked decisions):**
- `key={index}` MUST remain on `<article>`, NOT be moved to `<Card>` (Pitfall #3; D-01 keeps `<article>` outermost).
- Do NOT add any `className` prop to `<Card>` or `<CardContent>` (D-03/04/05).
- Do NOT introduce `<CardHeader>`, `<CardTitle>`, `<CardDescription>`, or `<CardFooter>` (D-02).
- Do NOT touch the inner `<div className="flex flex-col sm:flex-row ...">` at line 34, the headings, the date `<span>`, or the `<p className="mt-4 ...">` details paragraph. All preserved verbatim.
- The `mt-4` on the details paragraph is unaffected — it is a child of `<CardContent>`, not a direct child of `<Card>`, so Card's `gap-6` never applies (Finding #2 logic).
- Indentation: shift the inner block (lines 34-49 in the original) one additional level (two extra spaces) so Biome stays clean. Run `npm run format` after if needed.
  </action>
  <verify>
    <automated>test $(grep -c 'rounded-xl border border-zinc-200 bg-white p-6 shadow-sm' src/components/EducationSection.tsx) -eq 0 && test $(grep -c '<Card>' src/components/EducationSection.tsx) -ge 1 && test $(grep -c '<CardContent>' src/components/EducationSection.tsx) -ge 1 && test $(grep -c 'from "@/components/ui/card"' src/components/EducationSection.tsx) -eq 1 && test $(grep -c '<article key={index}>' src/components/EducationSection.tsx) -ge 1 && test $(grep -c 'mt-4' src/components/EducationSection.tsx) -ge 1 && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rounded-xl border border-zinc-200 bg-white p-6 shadow-sm" src/components/EducationSection.tsx` returns `0`
    - `grep -c "<Card>" src/components/EducationSection.tsx` returns `>= 1`
    - `grep -c "<CardContent>" src/components/EducationSection.tsx` returns `>= 1`
    - `grep -c 'from "@/components/ui/card"' src/components/EducationSection.tsx` returns `1`
    - `grep -c "<article key={index}>" src/components/EducationSection.tsx` returns `>= 1` (landmark + key on article per Pitfall #3)
    - `grep -E 'Card key=' src/components/EducationSection.tsx` returns no matches (key NOT on Card)
    - `grep -c "mt-4" src/components/EducationSection.tsx` returns `>= 1` (details paragraph margin preserved)
    - `grep -E 'Card className=|CardContent className=' src/components/EducationSection.tsx` returns no matches (no overrides)
    - `grep -E 'CardHeader|CardTitle|CardDescription|CardFooter' src/components/EducationSection.tsx` returns no matches (D-02)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>
    Every education entry renders as `<article key={index}><Card><CardContent>...</CardContent></Card></article>` with details `mt-4` preserved and key on `<article>`. Lint clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. JSX-only refactor.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Tampering | EducationSection.tsx `<article>` landmark | mitigate | Acceptance criterion `grep -c "<article key={index}>" >= 1` enforces landmark + correct key placement (D-01, Pitfall #3). |
| T-14-09 | Tampering | React list reconciliation (`key` prop) | mitigate | Acceptance criterion `grep -E 'Card key=' returns no matches` prevents key from being moved to Card (Pitfall #3). |
| T-14-10 | Information Disclosure | Card children (degree/institution/details) | accept | React default escaping; strings are YAML-authored and previously rendered. |
</threat_model>

<verification>
- Hand-rolled pattern gone: `grep -c "rounded-xl border border-zinc-200 bg-white p-6 shadow-sm" src/components/EducationSection.tsx` → 0
- Card primitive used: `grep -c "<Card>" src/components/EducationSection.tsx` → >= 1
- `key` on `<article>`: `grep -c "<article key={index}>" src/components/EducationSection.tsx` → >= 1
- `mt-4` details margin preserved: `grep -c "mt-4" src/components/EducationSection.tsx` → >= 1
- Lint clean: `npm run lint` → exit 0
</verification>

<success_criteria>
- CARD-03 satisfied: Each Education entry uses shadcn Card, hand-rolled classes removed.
- `<article key={index}>` landmark with key preserved (D-01, Pitfall #3).
- `mt-4` details margin preserved.
- No Card/CardContent className overrides.
- Biome lint passes.
</success_criteria>

<output>
After completion, create `.planning/phases/14-card-swap/14-03-SUMMARY.md` documenting:
- Diff applied (before/after snippets)
- Grep verification results (including confirmation key stayed on `<article>`)
- Lint result
</output>
