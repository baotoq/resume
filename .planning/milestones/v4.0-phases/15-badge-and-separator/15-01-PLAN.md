---
phase: 15-badge-and-separator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/techstack-icons/TechStackIcons.tsx
autonomous: true
requirements:
  - BADGE-01
tags:
  - shadcn
  - badge
  - refactor
  - visual-parity
must_haves:
  truths:
    - "TechStackIcons fallback branch renders shadcn <Badge variant=\"secondary\"> instead of hand-rolled zinc pill"
    - "Icon-hit branch (TECH_ICON_MAP matches) is unchanged — no Badge wrapper applied"
    - "Hand-rolled utility string 'bg-zinc-100 text-zinc-600 rounded-full px-2 py-0.5 text-xs' is gone from the file"
    - "Tech name text is preserved verbatim inside Badge children (no case transform, no truncation)"
  artifacts:
    - path: "src/components/techstack-icons/TechStackIcons.tsx"
      provides: "TechIcon fallback branch using <Badge variant=\"secondary\">"
      contains: "import { Badge } from \"@/components/ui/badge\""
  key_links:
    - from: "src/components/techstack-icons/TechStackIcons.tsx"
      to: "src/components/ui/badge.tsx"
      via: "named import Badge"
      pattern: "from \"@/components/ui/badge\""
---

<objective>
Replace the hand-rolled fallback pill in `src/components/techstack-icons/TechStackIcons.tsx` (lines 88-92 `else` return) with `<Badge variant="secondary">{tech}</Badge>` from the shadcn primitive installed in Phase 13. Satisfies BADGE-01. Per 15-CONTEXT D-01/D-02/D-03 and 15-UI-SPEC "Per-File JSX Contract — File 1": the icon-hit branch is NOT touched (SC#2 lock), no tooltip added, no case transform on `{tech}`.

Purpose: Remove the last hand-rolled pill utility string from the component tree — theme-token-driven visual with single source of truth.
Output: `src/components/techstack-icons/TechStackIcons.tsx` with the fallback branch using `<Badge variant="secondary">`, Badge import added, zero className overrides.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-badge-and-separator/15-CONTEXT.md
@.planning/phases/15-badge-and-separator/15-UI-SPEC.md
@.planning/phases/15-badge-and-separator/15-RESEARCH.md
@src/components/techstack-icons/TechStackIcons.tsx
@src/components/ui/badge.tsx

<interfaces>
<!-- From src/components/ui/badge.tsx — extracted export used by this plan -->
```tsx
function Badge({
  className,
  variant = "default",
  asChild = false,
  ...props
}: React.ComponentProps<"span"> &
  VariantProps<typeof badgeVariants> & { asChild?: boolean })

// variant="secondary" applies: "bg-secondary text-secondary-foreground [a&]:hover:bg-secondary/90"
// Base classes include: "inline-flex w-fit shrink-0 items-center justify-center gap-1 rounded-full border border-transparent px-2 py-0.5 text-xs font-medium"
// No "use client" directive — RSC-safe.

export { Badge, badgeVariants };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap TechStackIcons fallback branch to shadcn Badge (BADGE-01)</name>
  <files>src/components/techstack-icons/TechStackIcons.tsx</files>
  <read_first>
    - src/components/techstack-icons/TechStackIcons.tsx (the file being modified — current state at lines 73-93)
    - src/components/ui/badge.tsx (primitive source — confirm variant classes and RSC-safety)
    - .planning/phases/15-badge-and-separator/15-UI-SPEC.md (Per-File JSX Contract — File 1)
    - .planning/phases/15-badge-and-separator/15-CONTEXT.md (D-01, D-02, D-03, D-08 locked decisions)
  </read_first>
  <action>
Implement per 15-CONTEXT D-01/D-02/D-03 and 15-UI-SPEC.md "Per-File JSX Contract — File 1".

**Step 1 — Add import** at the top of the file. Add this line grouped with the other `@/components/ui/*` or project imports (after the icon-component imports from `./*Icon`):

```tsx
import { Badge } from "@/components/ui/badge";
```

**Step 2 — Replace ONLY the fallback `else` return branch** (currently lines 88-92 in `function TechIcon`).

BEFORE (lines 88-92):
```tsx
  return (
    <span className="bg-zinc-100 text-zinc-600 rounded-full px-2 py-0.5 text-xs">
      {tech}
    </span>
  );
```

AFTER:
```tsx
  return <Badge variant="secondary">{tech}</Badge>;
```

**Step 3 — Run formatter to settle import order:**

```bash
npm run format
```

**Constraints (per locked decisions):**
- Do NOT modify the `if (Icon)` branch at lines 77-86. The icon-hit return with `<div className="relative group">`, `<Icon size={40} />`, and absolute-positioned tooltip `<span>` is SC#2-locked (D-08 in CONTEXT).
- Do NOT add any `className` prop to `<Badge>` (per D-01 trust-tokens, mirrors Phase 14 D-04 precedent).
- Do NOT add a tooltip wrapper around the Badge (per D-03 — fallback keeps no tooltip).
- Do NOT transform `tech` — pass it verbatim as children (per D-02).
- Do NOT touch `TECH_ICON_MAP`, `normalizeTech`, `TechStackIconsProps`, `IconComponent`, or the `TechStackIcons` wrapper.

The fallback return now produces a single-line JSX expression — the `<Badge>` primitive applies `bg-secondary text-secondary-foreground rounded-full px-2 py-0.5 text-xs` by default, matching the prior pill shape with theme tokens instead of raw zinc classes.
  </action>
  <verify>
    <automated>test $(grep -c 'bg-zinc-100 text-zinc-600 rounded-full' src/components/techstack-icons/TechStackIcons.tsx) -eq 0 && test $(grep -c '<Badge variant="secondary">' src/components/techstack-icons/TechStackIcons.tsx) -ge 1 && test $(grep -c 'from "@/components/ui/badge"' src/components/techstack-icons/TechStackIcons.tsx) -eq 1 && test $(grep -c 'TECH_ICON_MAP' src/components/techstack-icons/TechStackIcons.tsx) -ge 2 && test $(grep -c 'relative group' src/components/techstack-icons/TechStackIcons.tsx) -ge 1 && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'bg-zinc-100 text-zinc-600 rounded-full' src/components/techstack-icons/TechStackIcons.tsx` returns `0` (hand-rolled pill gone)
    - `grep -c '<Badge variant="secondary">' src/components/techstack-icons/TechStackIcons.tsx` returns `>= 1` (Badge applied)
    - `grep -c '<Badge' src/components/techstack-icons/TechStackIcons.tsx` returns `1` (Badge appears exactly once — fallback branch only, not icon branch)
    - `grep -c 'from "@/components/ui/badge"' src/components/techstack-icons/TechStackIcons.tsx` returns `1` (single import)
    - `grep -c 'relative group' src/components/techstack-icons/TechStackIcons.tsx` returns `>= 1` (icon-branch tooltip wrapper preserved, SC#2 / D-08)
    - `grep -c 'TECH_ICON_MAP' src/components/techstack-icons/TechStackIcons.tsx` returns `>= 2` (map definition + lookup preserved)
    - `grep -E 'Badge className=' src/components/techstack-icons/TechStackIcons.tsx` returns no matches (no override, D-01 precedent)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>
    `TechStackIcons.tsx` fallback branch renders `<Badge variant="secondary">{tech}</Badge>`. Badge imported. Icon-hit branch and tooltip untouched. No className override on Badge. Biome lint clean. Hand-rolled zinc pill string eradicated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. No user input surfaces added. `tech` string flows from `resume.md` YAML → `ResumeData` type → component props → Badge children. Existing React default-escaping applies.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | TechStackIcons fallback branch | mitigate | Acceptance criteria `grep -c 'bg-zinc-100'` returns 0 and `grep -c '<Badge variant="secondary">'` returns ≥ 1 — enforces swap. Scope-lock criteria (`relative group` ≥ 1, `TECH_ICON_MAP` ≥ 2) guard icon branch from accidental modification (D-08). |
| T-15-02 | Tampering | Badge primitive token trust | mitigate | Acceptance criterion `grep -E 'Badge className='` returns no matches — enforces no override, preserves theme-token surface (D-01). |
| T-15-03 | Information Disclosure | `{tech}` string rendering | accept | React default HTML-escapes children; YAML-authored `tech` names were already rendered pre-swap. No new data path. |
</threat_model>

<verification>
- `grep -c 'bg-zinc-100 text-zinc-600 rounded-full' src/components/techstack-icons/TechStackIcons.tsx` → 0
- `grep -c '<Badge variant="secondary">' src/components/techstack-icons/TechStackIcons.tsx` → >= 1
- `grep -c '<Badge' src/components/techstack-icons/TechStackIcons.tsx` → 1
- `grep -c 'from "@/components/ui/badge"' src/components/techstack-icons/TechStackIcons.tsx` → 1
- `grep -c 'relative group' src/components/techstack-icons/TechStackIcons.tsx` → >= 1 (SC#2 lock)
- `npm run lint` → exit 0
- (Plan 15-03 gate runs full build + manual visual parity check)
</verification>

<success_criteria>
- BADGE-01 satisfied: fallback pill uses shadcn Badge variant="secondary".
- SC#1 satisfied: hand-rolled `bg-zinc-100 text-zinc-600 rounded-full` pattern gone.
- SC#2 satisfied: icon-hit branch unchanged (tooltip + `<Icon>` preserved).
- No className overrides on Badge (D-01).
- Biome lint passes.
</success_criteria>

<output>
After completion, create `.planning/phases/15-badge-and-separator/15-01-SUMMARY.md` documenting:
- The exact diff applied (import + fallback return swap)
- Grep verification output (zeros and positives)
- Lint result
- Any decisions exercised under "Claude's Discretion" (default: none)
</output>
