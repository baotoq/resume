---
phase: 15-badge-and-separator
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
autonomous: true
requirements:
  - SEP-01
tags:
  - shadcn
  - separator
  - layout
  - animation
must_haves:
  truths:
    - "page.tsx renders two <Separator /> elements between the three resume-section AnimateIn blocks"
    - "Each Separator is wrapped in AnimateIn with delay matching the following card (0.1 before Work, 0.2 before Education)"
    - "Header.tsx inline · dots are unchanged (SC#4 lock — not touched by this plan)"
    - "Separator import added from @/components/ui/separator, no className overrides"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Root page layout with two shadcn Separator elements between sections"
      contains: "import { Separator } from \"@/components/ui/separator\""
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/ui/separator.tsx"
      via: "named import Separator"
      pattern: "from \"@/components/ui/separator\""
---

<objective>
Insert two horizontal shadcn `<Separator />` elements in `src/app/page.tsx` between the three top-level resume sections (Header ↔ WorkExperience, WorkExperience ↔ EducationSection). Each Separator is wrapped in `<AnimateIn>` with `delay` matching the following card per 15-CONTEXT D-04/D-05/D-06 and 15-UI-SPEC "Per-File JSX Contract — File 2". Satisfies SEP-01.

Purpose: Add theme-token structural dividers between resume sections without touching the inline `·` contact-row separators in Header (SC#4-locked).
Output: `src/app/page.tsx` with two `<AnimateIn delay={…}><Separator /></AnimateIn>` blocks inserted into the existing flex-col container, Separator import added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-badge-and-separator/15-CONTEXT.md
@.planning/phases/15-badge-and-separator/15-UI-SPEC.md
@.planning/phases/15-badge-and-separator/15-RESEARCH.md
@src/app/page.tsx
@src/components/ui/separator.tsx
@src/components/animation/AnimateIn.tsx

<interfaces>
<!-- From src/components/ui/separator.tsx — extracted export used by this plan -->
```tsx
"use client";
function Separator({
  className,
  orientation = "horizontal",
  decorative = true,
  ...props
}: React.ComponentProps<typeof SeparatorPrimitive.Root>)

// Default classes: "shrink-0 bg-border data-[orientation=horizontal]:h-px data-[orientation=horizontal]:w-full"
// Token: bg-border = oklch(0.922 0 0) ≈ zinc-200

export { Separator };
```

Note: File declares `"use client"` but Separator is a pass-through primitive — importing it into a Server Component (page.tsx) is safe; React serializes the boundary automatically.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert two shadcn Separators into page.tsx between resume sections (SEP-01)</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (the file being modified — current state lines 1-40)
    - src/components/ui/separator.tsx (primitive source — confirm default horizontal classes)
    - .planning/phases/15-badge-and-separator/15-UI-SPEC.md (Per-File JSX Contract — File 2)
    - .planning/phases/15-badge-and-separator/15-CONTEXT.md (D-04, D-05, D-06, D-07, D-09 locked decisions)
  </read_first>
  <action>
Implement per 15-CONTEXT D-04/D-05/D-06/D-07/D-09 and 15-UI-SPEC.md "Per-File JSX Contract — File 2".

**Step 1 — Add import.** Insert this line grouped with the other `@/components/*` imports (near the existing `@/components/animation/AnimateIn` import):

```tsx
import { Separator } from "@/components/ui/separator";
```

**Step 2 — Insert two `<AnimateIn><Separator /></AnimateIn>` blocks into the flex-col container.**

BEFORE (inside `<div className="mx-auto max-w-3xl flex flex-col gap-8">`, currently lines 27-36):
```tsx
        <AnimateIn delay={0}>
          <Header resume={resume} email={email} phone={phone} />
        </AnimateIn>
        <AnimateIn delay={0.1}>
          <WorkExperience experience={resume.experience} />
        </AnimateIn>
        <AnimateIn delay={0.2}>
          <EducationSection education={resume.education ?? []} />
        </AnimateIn>
```

AFTER:
```tsx
        <AnimateIn delay={0}>
          <Header resume={resume} email={email} phone={phone} />
        </AnimateIn>
        <AnimateIn delay={0.1}>
          <Separator />
        </AnimateIn>
        <AnimateIn delay={0.1}>
          <WorkExperience experience={resume.experience} />
        </AnimateIn>
        <AnimateIn delay={0.2}>
          <Separator />
        </AnimateIn>
        <AnimateIn delay={0.2}>
          <EducationSection education={resume.education ?? []} />
        </AnimateIn>
```

**Step 3 — Run formatter to settle import order:**

```bash
npm run format
```

**Constraints (per locked decisions):**
- Exactly TWO `<Separator />` elements — one before WorkExperience AnimateIn, one before EducationSection AnimateIn (D-04).
- Do NOT insert a Separator before the Header AnimateIn or after the EducationSection AnimateIn (D-04 locks "between three sections" = two gaps).
- Each Separator MUST be wrapped in its own `<AnimateIn>` with `delay` matching the following card: `0.1` before WorkExperience, `0.2` before EducationSection (D-05).
- Do NOT add any `className` prop to `<Separator />` (D-06 trust-tokens).
- Do NOT pass `orientation`, `decorative`, or any other prop — defaults are correct (D-06).
- Do NOT modify `main`, the outer `<div>` container, the `resume.md` read logic, or any existing `<AnimateIn delay={0/0.1/0.2}>` wrapper.
- Do NOT modify `src/components/Header.tsx` (D-07 scope-lock — inline `·` dots stay as text per SC#4).

The three existing cards remain untouched; two new AnimateIn-wrapped Separators are interleaved between them.
  </action>
  <verify>
    <automated>test $(grep -c '<Separator />' src/app/page.tsx) -eq 2 && test $(grep -c 'from "@/components/ui/separator"' src/app/page.tsx) -eq 1 && test $(grep -c '<AnimateIn delay={0.1}>' src/app/page.tsx) -eq 2 && test $(grep -c '<AnimateIn delay={0.2}>' src/app/page.tsx) -eq 2 && test $(grep -c '<AnimateIn delay={0}>' src/app/page.tsx) -eq 1 && test $(grep -c 'Separator className=' src/app/page.tsx) -eq 0 && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<Separator />' src/app/page.tsx` returns `2` (exactly two separators, D-04)
    - `grep -c 'from "@/components/ui/separator"' src/app/page.tsx` returns `1` (single import)
    - `grep -c '<AnimateIn delay={0.1}>' src/app/page.tsx` returns `2` (one for Separator, one for WorkExperience — D-05)
    - `grep -c '<AnimateIn delay={0.2}>' src/app/page.tsx` returns `2` (one for Separator, one for EducationSection — D-05)
    - `grep -c '<AnimateIn delay={0}>' src/app/page.tsx` returns `1` (Header's AnimateIn, unchanged)
    - `grep -c 'Separator className=' src/app/page.tsx` returns `0` (no override, D-06)
    - `grep -c 'orientation=' src/app/page.tsx` returns `0` (default horizontal, D-06)
    - `git diff --name-only | grep -c 'src/components/Header.tsx'` returns `0` (Header untouched, D-07 / SC#4)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>
    `src/app/page.tsx` renders five children inside the flex-col container in order: Header-AnimateIn, Separator-AnimateIn (delay 0.1), WorkExperience-AnimateIn (delay 0.1), Separator-AnimateIn (delay 0.2), EducationSection-AnimateIn (delay 0.2). Separator imported. No className props. Header.tsx untouched. Biome lint clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. No user input surfaces added; this plan inserts decorative divider markup into a Server Component page.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | page.tsx AnimateIn delay values | mitigate | Acceptance criteria enforce `<AnimateIn delay={0.1}>` count = 2 and `<AnimateIn delay={0.2}>` count = 2 — prevents Separator placement with wrong delay (D-05). |
| T-15-05 | Tampering | Header.tsx SC#4 lock | mitigate | Acceptance criterion `git diff` has no Header.tsx entry — enforces D-07 scope-lock so inline `·` dots are not replaced with vertical Separator. |
| T-15-06 | Tampering | Separator primitive token trust | mitigate | Acceptance criteria `Separator className=` count = 0 and `orientation=` count = 0 — enforces default token usage (D-06). |
| T-15-07 | Denial of Service | "use client" client-bundle growth | accept | Radix Separator is ~0.5KB minified + gzipped; no meaningful bundle regression. Phase 13 installation already included the package. |
</threat_model>

<verification>
- `grep -c '<Separator />' src/app/page.tsx` → 2
- `grep -c 'from "@/components/ui/separator"' src/app/page.tsx` → 1
- `grep -c '<AnimateIn delay={0.1}>' src/app/page.tsx` → 2
- `grep -c '<AnimateIn delay={0.2}>' src/app/page.tsx` → 2
- `grep -c '<AnimateIn delay={0}>' src/app/page.tsx` → 1
- `grep -c 'Separator className=' src/app/page.tsx` → 0
- `npm run lint` → exit 0
- (Plan 15-03 gate runs full build + manual visual verification)
</verification>

<success_criteria>
- SEP-01 satisfied: two horizontal shadcn Separators present between resume sections.
- SC#3 satisfied: Separators visible in browser between cards (verified in Plan 15-03 manual check).
- SC#4 satisfied: Header.tsx `·` text dots untouched (scope-lock).
- D-05 animation coupling: each Separator shares delay with following card.
- Biome lint passes.
</success_criteria>

<output>
After completion, create `.planning/phases/15-badge-and-separator/15-02-SUMMARY.md` documenting:
- The exact diff applied (import + 5-child flex-col layout)
- Grep verification output (all counts)
- Lint result
- Any decisions exercised under "Claude's Discretion" (default: none — no overrides, default delays)
</output>
