---
phase: 08-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - biome.json
  - package.json
  - src/app/page.tsx
  - src/components/resume/Projects.tsx
  - src/data/github.ts
  - src/data/githubColors.ts
  - src/data/resume.ts
  - src/types/resume.ts
  - src/styles/print.css
  - scripts/fetch-github.ts
  - public/file.svg
  - public/globe.svg
  - public/next.svg
  - public/vercel.svg
  - public/window.svg
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04

must_haves:
  truths:
    - "No unused SVG files remain in public/ directory"
    - "No dead Projects feature code remains in codebase"
    - "No stale CSS selectors for removed classes in print.css"
    - "Lint passes cleanly (biome check exits 0)"
    - "Build succeeds and output size is <= 1.1M"
  artifacts:
    - path: "biome.json"
      provides: "Fixed Biome config without broken glob pattern"
      contains: "includes"
    - path: "src/app/page.tsx"
      provides: "Clean page without Projects imports or showProjects flag"
    - path: "src/styles/print.css"
      provides: "Print styles with no stale selectors"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/resume/"
      via: "imports"
      pattern: "import.*from.*@/components/resume"
    - from: "src/data/resume.ts"
      to: "src/types/resume.ts"
      via: "type imports"
      pattern: "import type.*from.*@/types/resume"
---

<objective>
Remove all identified dead code, unused assets, and stale CSS from the codebase. Fix the broken Biome config to restore linting. Verify build still succeeds with same or smaller output size.

Purpose: Final cleanup pass before shipping v1 -- remove Next.js starter template SVGs, the disabled Projects feature chain, stale print CSS selectors, and fix the broken Biome glob pattern.
Output: A clean codebase with no dead code, passing lint, and successful build.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cleanup/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Biome config, remove unused SVGs, remove Projects feature chain</name>
  <files>biome.json, public/file.svg, public/globe.svg, public/next.svg, public/vercel.svg, public/window.svg, src/app/page.tsx, src/components/resume/Projects.tsx, src/data/github.ts, src/data/githubColors.ts, src/data/resume.ts, src/types/resume.ts, scripts/fetch-github.ts, package.json</files>
  <action>
Execute in this exact order:

1. **Fix biome.json** (CLEAN-04 prerequisite): Remove the broken glob `!!**/.git` from `files.includes` array (line 16). The array should only contain the `src/**` pattern and the `!**/node_modules/**/*` pattern.

2. **Delete 5 unused SVGs** (CLEAN-01): Remove these Next.js starter template files that have zero references in src/:
   - `public/file.svg`
   - `public/globe.svg`
   - `public/next.svg`
   - `public/vercel.svg`
   - `public/window.svg`

3. **Remove Projects feature from page.tsx** (CLEAN-02): In `src/app/page.tsx`:
   - Remove import of `ProjectsSection` from `@/components/resume/Projects` (line 9)
   - Remove import of `githubRepos` from `@/data/github` (line 14)
   - Remove the `showProjects` constant (line 18)
   - Remove the `{showProjects && <ProjectsSection repos={githubRepos} />}` JSX (line 72)

4. **Delete dead feature files** (CLEAN-02):
   - Delete `src/components/resume/Projects.tsx` (entire component)
   - Delete `src/data/github.ts` (generated GitHub repos data)
   - Delete `src/data/githubColors.ts` (language color map)
   - Delete `scripts/fetch-github.ts` (prebuild fetch script)

5. **Remove `Project` interface from types** (CLEAN-02): In `src/types/resume.ts`, remove the `Project` interface (lines ~41-45). Keep all other interfaces.

6. **Remove `projects` export from data** (CLEAN-02): In `src/data/resume.ts`, remove the `projects` array export (around line 150) and its import of the `Project` type. Keep all other exports.

7. **Remove `prebuild` script from package.json** (CLEAN-02): Remove the `"prebuild": "tsx scripts/fetch-github.ts"` line from the scripts section. Keep all other scripts.

8. **Verify**: Run `npm run lint` (should now pass with fixed biome.json) and `npm run build` (should succeed).
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/resume && npm run lint && npm run build && ls public/*.svg 2>/dev/null | wc -l | tr -d ' '</automated>
  </verify>
  <done>Biome lint passes (exit 0). Build succeeds. `ls public/*.svg` returns 0 files. No ProjectsSection, githubRepos, githubColors, fetch-github, showProjects, or Project interface remain in codebase. Prebuild script removed from package.json.</done>
</task>

<task type="auto">
  <name>Task 2: Clean stale print CSS selectors and final verification</name>
  <files>src/styles/print.css</files>
  <action>
Remove three groups of stale CSS selectors from `src/styles/print.css` that target classes no longer used by any component:

1. **Remove `.bg-clip-text` block** (lines 121-127): The `@media print` rule for `.bg-clip-text` — no component uses this class.

2. **Remove `.ant-card` blocks** (lines 130-144): Three rules targeting `.ant-card`, `.ant-card.border-l-4`, and `.ant-card-body` — Ant Design Card is not used anywhere (only icons are used from antd).

3. **Remove `section.avoid-break` block** (lines 36-39): The rule for `section.avoid-break` — no element has the `avoid-break` class.

Keep all other print CSS rules intact. They are actively used by current components.

After editing, run full verification suite:
- `npm run lint` — confirms no lint issues
- `npm run build` — confirms static export works
- `du -sh out/` — confirms build size <= 1.1M
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/resume && npm run lint && npm run build && du -sh out/ && ! grep -q "bg-clip-text\|ant-card\|avoid-break" src/styles/print.css && echo "CLEAN"</automated>
  </verify>
  <done>print.css has no stale selectors (bg-clip-text, ant-card, avoid-break all removed). Lint passes. Build succeeds. Build output size <= 1.1M. All existing features (theme toggle, PDF export, all sections) remain functional.</done>
</task>

</tasks>

<verification>
1. `npm run lint` exits 0 (biome config fixed, no lint errors)
2. `npm run build` exits 0 (static export succeeds)
3. `du -sh out/` shows <= 1.1M
4. `ls public/*.svg 2>/dev/null` returns nothing (all starter SVGs removed)
5. `grep -r "ProjectsSection\|githubRepos\|githubColors\|showProjects" src/` returns nothing
6. `grep -r "Project" src/types/resume.ts` does not match a `Project` interface
7. `grep "prebuild" package.json` returns nothing
8. `grep "bg-clip-text\|ant-card\|avoid-break" src/styles/print.css` returns nothing
</verification>

<success_criteria>
- Zero unused SVG files in public/ (CLEAN-01)
- Zero dead Projects feature code in codebase (CLEAN-02)
- Zero stale CSS selectors in print.css (CLEAN-03)
- Lint and build both pass cleanly (CLEAN-04)
- Build output size <= 1.1M (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/08-cleanup/08-01-SUMMARY.md`
</output>
