---
phase: 06-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/app/layout.tsx
autonomous: true
requirements:
  - ANA-01
  - ANA-02
  - ANA-03
must_haves:
  truths:
    - "Plausible script tag appears in production build HTML"
    - "Script loads asynchronously and does not block page rendering"
    - "No cookies are set by the analytics script"
    - "Page views are tracked automatically on navigation"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "PlausibleProvider integration in root layout"
      contains: "PlausibleProvider"
    - path: "package.json"
      provides: "next-plausible dependency"
      contains: "next-plausible"
  key_links:
    - from: "src/app/layout.tsx"
      to: "plausible.io/js/script.js"
      via: "PlausibleProvider component injects script tag"
      pattern: "PlausibleProvider.*domain"
user_setup:
  - service: plausible
    why: "Privacy-friendly analytics dashboard"
    account_setup:
      - task: "Create Plausible account (if not already done)"
        location: "https://plausible.io/register"
      - task: "Register site with domain baotoq.github.io/resume"
        location: "Plausible Dashboard -> Add a website"
---

<objective>
Integrate Plausible Analytics into the resume site for privacy-friendly visitor tracking.

Purpose: Track page views to understand visitor engagement without compromising user privacy (no cookies, GDPR compliant).
Output: Working Plausible integration via next-plausible package in root layout.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-analytics/06-RESEARCH.md

<interfaces>
<!-- Current layout.tsx structure the executor will modify -->
From src/app/layout.tsx:
```typescript
import type { Metadata } from "next";
import { Plus_Jakarta_Sans } from "next/font/google";
import { AntdRegistry } from "@ant-design/nextjs-registry";
import { ThemeProvider } from "@/components/providers/ThemeProvider";
import "./globals.css";

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${plusJakartaSans.variable} antialiased`}>
        <ThemeProvider>
          <AntdRegistry>{children}</AntdRegistry>
        </ThemeProvider>
      </body>
    </html>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install next-plausible and add PlausibleProvider to root layout</name>
  <files>package.json, package-lock.json, src/app/layout.tsx</files>
  <action>
1. Install the next-plausible package:
   ```bash
   npm install next-plausible
   ```

2. Modify `src/app/layout.tsx`:
   - Add import: `import PlausibleProvider from "next-plausible";`
   - Add a `<head>` element inside the `<html>` tag, before `<body>`
   - Place `<PlausibleProvider domain="baotoq.github.io/resume" />` inside `<head>`
   - Do NOT use proxy/customDomain (incompatible with static export)
   - Do NOT add trackLocalhost (production-only tracking is correct default)

The resulting JSX structure should be:
```tsx
<html lang="en" suppressHydrationWarning>
  <head>
    <PlausibleProvider domain="baotoq.github.io/resume" />
  </head>
  <body className={`${plusJakartaSans.variable} antialiased`}>
    <ThemeProvider>
      <AntdRegistry>{children}</AntdRegistry>
    </ThemeProvider>
  </body>
</html>
```

3. Run `npm run lint` and `npm run build` to verify no errors.
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/resume && npm run lint && npm run build && grep -q "plausible" out/index.html && echo "PASS: Plausible script found in build output"</automated>
  </verify>
  <done>
- next-plausible is in package.json dependencies
- PlausibleProvider is in layout.tsx with domain="baotoq.github.io/resume"
- Production build output (out/index.html) contains the Plausible script tag referencing plausible.io/js/script.js
- Lint passes, build succeeds with no errors
- No cookies set (inherent to Plausible -- no code needed)
  </done>
</task>

</tasks>

<verification>
1. `npm run lint` passes without errors
2. `npm run build` succeeds (static export to out/)
3. `grep "plausible" out/index.html` shows the script tag with correct domain
4. Script tag includes `data-domain="baotoq.github.io/resume"`
5. Script loads with `async` or `defer` attribute (non-blocking)
6. No proxy or customDomain configuration present
</verification>

<success_criteria>
- Plausible script tag present in production HTML with domain="baotoq.github.io/resume"
- Build and lint pass cleanly
- Script does not block page rendering (async loading)
- No cookies are used (Plausible is cookie-free by design)
- Once user registers site in Plausible dashboard, page views will appear
</success_criteria>

<output>
After completion, create `.planning/phases/06-analytics/06-01-SUMMARY.md`
</output>
