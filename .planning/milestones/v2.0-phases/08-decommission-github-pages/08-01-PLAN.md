---
phase: 08-decommission-github-pages
plan: 08-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/components/LogoImage.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "fs.readFileSync failure in page.tsx throws a clear error instead of crashing with an unhandled ENOENT"
    - "LogoImage props interface extends HTMLAttributes<HTMLDivElement>, not ButtonHTMLAttributes"
    - "npm run build passes with no TypeScript errors"
    - "npm run lint passes with no warnings on modified files"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Guarded file read with try/catch around readFileSync"
      contains: "try {"
    - path: "src/components/LogoImage.tsx"
      provides: "Correct div wrapper props interface"
      contains: "React.HTMLAttributes<HTMLDivElement>"
  key_links:
    - from: "src/app/page.tsx"
      to: "Next.js error boundary"
      via: "throw new Error()"
      pattern: "throw new Error"
    - from: "src/components/LogoImage.tsx"
      to: "HTMLDivElement attributes"
      via: "interface extension"
      pattern: "HTMLAttributes<HTMLDivElement>"
---

<objective>
Fix two code review warnings from Phase 7: guard the synchronous file read in page.tsx (WR-01) and correct the TypeScript interface on LogoImage (WR-02).

Purpose: Eliminate the crash risk from an unguarded readFileSync and remove invalid button attributes from a div wrapper component.
Output: Two modified source files, build and lint clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-vercel-setup-config-migration/07-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix WR-01 — guard readFileSync in page.tsx with try/catch (per D-06)</name>
  <read_first>src/app/page.tsx</read_first>
  <files>src/app/page.tsx</files>
  <action>
Replace the bare readFileSync call on line 12 with a guarded block. The exact replacement:

BEFORE (lines 11-13):
```ts
const filePath = path.join(process.cwd(), "src/data/resume.md");
const raw = fs.readFileSync(filePath, "utf-8");
const { data } = matter(raw);
```

AFTER:
```ts
const filePath = path.join(process.cwd(), "src/data/resume.md");
let raw: string;
try {
  raw = fs.readFileSync(filePath, "utf-8");
} catch (err) {
  console.error("Failed to read resume.md:", err);
  throw new Error("Resume data unavailable");
}
const { data } = matter(raw);
```

No other changes to page.tsx. Do NOT create src/app/error.tsx — it is not in scope for this phase. Next.js App Router will catch the thrown Error and display a default error page.
  </action>
  <verify>
    <automated>grep -n "try {" src/app/page.tsx && grep -n "throw new Error" src/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- `grep "try {" src/app/page.tsx` returns a match
- `grep 'throw new Error("Resume data unavailable")' src/app/page.tsx` returns a match
- `grep "fs.readFileSync" src/app/page.tsx` still returns a match (the call is inside the try block, not removed)
- No bare `const raw = fs.readFileSync` line exists (the unguarded form is gone)
  </acceptance_criteria>
  <done>readFileSync call is wrapped in try/catch; failure throws Error("Resume data unavailable")</done>
</task>

<task type="auto">
  <name>Task 2: Fix WR-02 — correct LogoImage props interface to HTMLAttributes (per D-07)</name>
  <read_first>src/components/LogoImage.tsx</read_first>
  <files>src/components/LogoImage.tsx</files>
  <action>
Change line 6 only. The exact replacement:

BEFORE:
```ts
interface LogoImageProps extends React.ButtonHTMLAttributes<HTMLDivElement> {
```

AFTER:
```ts
interface LogoImageProps extends React.HTMLAttributes<HTMLDivElement> {
```

No other changes to LogoImage.tsx. The rest of the interface body (`src: string | undefined; alt: string;`) and all component logic remain identical.
  </action>
  <verify>
    <automated>grep -n "HTMLAttributes" src/components/LogoImage.tsx</automated>
  </verify>
  <acceptance_criteria>
- `grep "React.HTMLAttributes<HTMLDivElement>" src/components/LogoImage.tsx` returns a match on line 6
- `grep "ButtonHTMLAttributes" src/components/LogoImage.tsx` returns no match
  </acceptance_criteria>
  <done>LogoImageProps extends React.HTMLAttributes&lt;HTMLDivElement&gt;; no ButtonHTMLAttributes reference remains</done>
</task>

<task type="auto">
  <name>Task 3: Verify build and lint pass</name>
  <read_first></read_first>
  <files></files>
  <action>
Run the following commands in sequence. Both must exit 0.

1. `npm run lint`
2. `npm run build`

If either fails, fix the specific error before proceeding. Do not move to plan 08-02 until both pass.
  </action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
- `npm run lint` exits with code 0 and produces no errors on src/app/page.tsx or src/components/LogoImage.tsx
- `npm run build` exits with code 0 with no TypeScript compilation errors
  </acceptance_criteria>
  <done>Build and lint clean after both code fixes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → page.tsx | resume.md read at server render time; failure now throws instead of crashing silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Denial of Service | page.tsx readFileSync | mitigate | try/catch throws Error with message; page fails gracefully to Next.js default error page rather than hanging or crashing the server process |
| T-08-02 | Tampering | LogoImage props spread | accept | HTMLAttributes fix removes invalid button props from div; no security surface change, purely a type correctness fix |
</threat_model>

<verification>
After both tasks complete:
- `grep "try {" src/app/page.tsx` — match exists
- `grep 'throw new Error("Resume data unavailable")' src/app/page.tsx` — match exists
- `grep "ButtonHTMLAttributes" src/components/LogoImage.tsx` — no match
- `grep "React.HTMLAttributes<HTMLDivElement>" src/components/LogoImage.tsx` — match exists
- `npm run lint && npm run build` — exits 0
</verification>

<success_criteria>
Both code review warnings (WR-01, WR-02) are resolved. Build and lint pass. No new files created.
</success_criteria>

<output>
After completion, create `.planning/phases/08-decommission-github-pages/08-01-SUMMARY.md`
</output>
