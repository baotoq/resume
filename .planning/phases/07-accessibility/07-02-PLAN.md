---
phase: 07-accessibility
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: []
autonomous: false
requirements:
  - A11Y-01
  - A11Y-02
  - A11Y-03
  - A11Y-04
  - A11Y-05
  - A11Y-06

must_haves:
  truths:
    - "Skip link visible on Tab, jumps to main content"
    - "Focus rings visible on all interactive elements"
    - "Lighthouse accessibility score >= 95"
    - "No critical axe issues"
  artifacts: []
  key_links: []
---

<objective>
Verify all accessibility improvements through keyboard testing and Lighthouse audit.

Purpose: Confirm that all A11Y requirements are met through interactive testing.
Output: Verified accessibility compliance, Lighthouse score >= 95.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-accessibility/07-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify accessibility improvements</name>
  <files></files>
  <action>
Human verifies all accessibility improvements implemented in Plan 01.

What was built: Skip navigation link, global focus indicators, aria-labels on icon buttons, semantic landmarks (main element), contrast fixes (muted-foreground darkened), decorative element hiding (aria-hidden on quotes/bullets/timeline), and theme-aware focus rings on ThemeToggle.
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/resume && npm run build</automated>
  </verify>
  <done>All accessibility requirements verified through interactive testing. Lighthouse score >= 95.</done>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000/resume
    2. **Skip link (A11Y-01):** Press Tab once -- a "Skip to main content" link should appear at top-left. Press Enter -- focus should jump to the resume content area.
    3. **Focus indicators (A11Y-02):** Continue pressing Tab through the page. Every link and button should show a visible teal/accent-colored focus ring.
    4. **Aria-labels (A11Y-03):** Tab to the PDF download button -- inspect it in DevTools and confirm `aria-label="Download resume as PDF"` is present.
    5. **Contrast (A11Y-04):** Visually check that muted/gray text is readable. Run Lighthouse accessibility audit in Chrome DevTools (Inspect > Lighthouse > Accessibility) -- score should be >= 95.
    6. **Keyboard navigation (A11Y-05):** Navigate the entire page with Tab only. All links (company names, LinkedIn, GitHub) and buttons (theme toggle, PDF export) should be reachable.
    7. **Theme toggle (A11Y-06):** Tab to the theme toggle. Press Enter or Space -- theme should switch. The focus ring should be accent-colored (teal), not blue.
    8. **Dark mode:** Switch to dark mode and repeat steps 2-3 to confirm focus rings work in dark theme too.
    9. **Print:** Click the PDF export button and verify the skip link does NOT appear in the print preview.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Human confirms all accessibility requirements through interactive testing.
</verification>

<success_criteria>
- Lighthouse accessibility score >= 95
- All interactive elements keyboard-navigable
- Skip link works correctly
- Focus indicators visible in both themes
- No critical accessibility issues
</success_criteria>

<output>
After completion, create `.planning/phases/07-accessibility/07-02-SUMMARY.md`
</output>
