---
phase: 05.1-contact-info-privacy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/print.css
  - src/components/resume/Header.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - PRIVACY-01
must_haves:
  truths:
    - "Email and phone are NOT visible on the web page"
    - "Email and phone ARE visible in print/PDF output"
    - "Email and phone are NOT in JSON-LD structured data"
    - "LinkedIn and GitHub links remain visible on the web"
    - "Build and lint pass without errors"
  artifacts:
    - path: "src/styles/print.css"
      provides: ".print-only CSS class for screen-hidden, print-visible elements"
      contains: "print-only"
    - path: "src/components/resume/Header.tsx"
      provides: "Email and phone contact pills with print-only class"
      contains: "print-only"
    - path: "src/app/page.tsx"
      provides: "JSON-LD without email or telephone fields"
  key_links:
    - from: "src/styles/print.css"
      to: "src/components/resume/Header.tsx"
      via: ".print-only class applied to email/phone elements"
      pattern: "print-only"
    - from: "src/app/page.tsx"
      to: "JSON-LD output"
      via: "email and telephone removed from mainEntity"
      pattern: "email.*contactInfo|telephone.*contactInfo"
---

<objective>
Hide phone number and email from the public web version while keeping them in PDF/print export. Also remove email/telephone from JSON-LD structured data to prevent search engine indexing.

Purpose: Protect personal contact info from public web exposure and scraping while maintaining full contact details in the PDF resume.
Output: Modified Header.tsx, print.css, and page.tsx with privacy-preserving contact display.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-contact-info-privacy-hide-phone-email-from-web-keep-in-pdf-only/05.1-RESEARCH.md

@src/styles/print.css
@src/components/resume/Header.tsx
@src/app/page.tsx

<interfaces>
<!-- Current Header contact rendering pattern (Header.tsx lines 43-85) -->
<!-- Each contact item is an <a> with className containing flex items-center gap-2 px-4 py-2 bg-[var(--muted)] ... rounded-full ... -->
<!-- Email: lines 44-50, Phone: lines 52-60, LinkedIn: lines 62-72, GitHub: lines 74-84 -->

<!-- Current print.css pattern (line 15-17) -->
```css
.no-print {
  display: none !important;
}
```
<!-- The .print-only class is the inverse of .no-print -->

<!-- Current JSON-LD in page.tsx (lines 36-37) -->
```tsx
email: contactInfo.email,
telephone: contactInfo.phone,
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add print-only CSS class and apply to email/phone in Header</name>
  <files>src/styles/print.css, src/components/resume/Header.tsx</files>
  <action>
1. In `src/styles/print.css`, add a `.print-only` rule OUTSIDE the `@media print` block (at the top of the file, before line 2):
   ```css
   /* Hide print-only elements on screen */
   .print-only {
     display: none !important;
   }
   ```

2. Inside the existing `@media print` block (after the `.no-print` rule around line 17), add:
   ```css
   .print-only {
     display: flex !important;
   }
   ```

3. In `src/components/resume/Header.tsx`, add the `print-only` class to the email `<a>` element (line 45). Change the className from:
   `"group flex items-center gap-2 px-4 py-2 bg-[var(--muted)] hover:bg-[var(--accent)]/10 rounded-full border border-[var(--border)] hover:border-[var(--accent)]/50 transition-all duration-200"`
   to:
   `"print-only flex items-center gap-2 px-4 py-2 bg-[var(--muted)] rounded-full border border-[var(--border)]"`
   (Remove `group`, `hover:` styles, and `transition-all duration-200` since the element is never visible on screen.)

4. Similarly, add `print-only` class to the phone `<a>` element (line 54). Change className to:
   `"print-only flex items-center gap-2 px-4 py-2 bg-[var(--muted)] rounded-full border border-[var(--border)]"`
   (Same rationale: remove hover/transition styles.)

5. Also remove `group-hover:text-[var(--accent)]` from the `<span>` inside both email and phone elements since hover is irrelevant for print-only elements.

Do NOT touch the LinkedIn or GitHub contact items -- they remain fully visible on web.
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/resume && npm run lint && npm run build</automated>
  </verify>
  <done>Email and phone contact pills have `print-only` class. They are hidden on screen via `display: none` and visible in print via `display: flex`. LinkedIn and GitHub remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Remove email and telephone from JSON-LD structured data</name>
  <files>src/app/page.tsx</files>
  <action>
In `src/app/page.tsx`, remove the two lines from the JSON-LD `mainEntity` object:
- Line 36: `email: contactInfo.email,`
- Line 37: `telephone: contactInfo.phone,`

Delete both lines entirely. Do not comment them out. The remaining properties (name, jobTitle, description, url, image, sameAs) stay intact.
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/resume && npm run build && ! grep -q '"email"' out/index.html && ! grep -q '"telephone"' out/index.html && echo "PASS: no email/telephone in output"</automated>
  </verify>
  <done>JSON-LD in the built output contains no email or telephone fields. The structured data remains valid with Person schema properties (name, jobTitle, description, url, image, sameAs).</done>
</task>

</tasks>

<verification>
1. `npm run lint` passes with no errors
2. `npm run build` succeeds (static export to /out)
3. Email and phone text do NOT appear in the rendered HTML body of the web page (only in JS bundle, which is acceptable)
4. Email and phone do NOT appear in the JSON-LD `<script>` block in the built HTML
5. Print preview (Cmd+P) shows email and phone contact pills in the header
</verification>

<success_criteria>
- Web page shows only LinkedIn and GitHub contact links (email and phone hidden)
- PDF/print export shows all contact info including email and phone
- JSON-LD structured data has no email or telephone properties
- Build succeeds, lint passes, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-contact-info-privacy-hide-phone-email-from-web-keep-in-pdf-only/05.1-01-SUMMARY.md`
</output>
