---
phase: 16-token-unification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/EducationSection.tsx
autonomous: true
requirements:
  - TOKEN-01
tags:
  - tokens
  - refactor
  - visual-parity
  - education
must_haves:
  truths:
    - "EducationSection.tsx contains zero raw text-zinc-*, border-zinc-*, bg-zinc-* classes"
    - "Section H2, degree/program H3, school name use text-foreground"
    - "Date range span uses text-muted-foreground"
    - "Description paragraph uses text-foreground"
    - "Layout class sm:text-right preserved"
  artifacts:
    - path: "src/components/EducationSection.tsx"
      provides: "Color-token-driven education section"
      contains: "text-foreground"
  key_links: []
---

<objective>
Replace all raw zinc color classes in `src/components/EducationSection.tsx` with shadcn semantic tokens per 16-CONTEXT.md D-01/D-02/D-03 and 16-UI-SPEC.md "Per-File Contracts — File 3". Five lines modified. No JSX, layout, or typography changes.

Purpose: Eliminate TOKEN-01 violations in Education component.
Output: `src/components/EducationSection.tsx` with zero raw color classes, biome lint clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-token-unification/16-CONTEXT.md
@.planning/phases/16-token-unification/16-UI-SPEC.md
@.planning/phases/16-token-unification/16-RESEARCH.md
@src/components/EducationSection.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap raw zinc color classes in EducationSection.tsx to shadcn tokens (TOKEN-01)</name>
  <files>src/components/EducationSection.tsx</files>
  <read_first>
    - src/components/EducationSection.tsx (confirm current state — 5 lines to modify: 27, 37, 40, 44, 49)
    - src/app/globals.css (confirm :root tokens foreground, muted-foreground exist)
    - .planning/phases/16-token-unification/16-UI-SPEC.md (File 3 contract table)
    - .planning/phases/16-token-unification/16-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <action>
Apply these exact substitutions. Only color-related class tokens change. All layout (`sm:text-right`, `mt-4`) and typography (`text-lg`, `text-base`, `text-sm`, `font-semibold`, `leading-relaxed`, `mb-6`) classes preserved.

**Line 27** — section H2:
BEFORE:
```tsx
<h2 className="text-xl font-semibold leading-[1.2] text-zinc-900 mb-6">
```
AFTER:
```tsx
<h2 className="text-xl font-semibold leading-[1.2] text-foreground mb-6">
```

**Line 37** — degree/program H3:
BEFORE:
```tsx
<h3 className="text-lg font-semibold text-zinc-900">
```
AFTER:
```tsx
<h3 className="text-lg font-semibold text-foreground">
```

**Line 40** — school name paragraph:
BEFORE:
```tsx
<p className="text-base text-zinc-700">
```
AFTER:
```tsx
<p className="text-base text-foreground">
```

**Line 44** — date range `<span>`:
BEFORE:
```tsx
<span className="text-sm font-semibold text-zinc-500 sm:text-right">
```
AFTER:
```tsx
<span className="text-sm font-semibold text-muted-foreground sm:text-right">
```

**Line 49** — description paragraph:
BEFORE:
```tsx
<p className="mt-4 text-base leading-relaxed text-zinc-700">
```
AFTER:
```tsx
<p className="mt-4 text-base leading-relaxed text-foreground">
```

**Constraints:**
- Do NOT add imports.
- Do NOT touch typography, spacing, responsive (`sm:text-right`) classes.
- Do NOT touch JSX structure.
- Do NOT introduce `cn()`.

After edits:
```bash
npm run format
```
  </action>
  <verify>
    <automated>test $(rg -c "text-zinc-|border-zinc-|bg-zinc-|text-indigo-|bg-indigo-|outline-indigo-|bg-blue-|text-blue-|bg-white|text-white" src/components/EducationSection.tsx || echo 0) -eq 0 && test $(grep -c "text-foreground" src/components/EducationSection.tsx) -ge 4 && test $(grep -c "text-muted-foreground" src/components/EducationSection.tsx) -ge 1 && test $(grep -c "sm:text-right" src/components/EducationSection.tsx) -eq 1 && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "text-zinc-|border-zinc-|bg-zinc-|text-indigo-|bg-indigo-|outline-indigo-|bg-blue-|text-blue-|bg-white|text-white" src/components/EducationSection.tsx` returns 0 / no matches
    - `grep -c "text-foreground" src/components/EducationSection.tsx` returns `>= 4` (H2 + H3 + school + description)
    - `grep -c "text-muted-foreground" src/components/EducationSection.tsx` returns `>= 1` (date range)
    - `grep -c "sm:text-right" src/components/EducationSection.tsx` returns `1` (responsive alignment preserved)
    - `grep -c "mt-4" src/components/EducationSection.tsx` returns `>= 1` (description spacing preserved)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>
    EducationSection.tsx uses only shadcn semantic tokens for color. Layout and typography preserved. Biome clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. Server Component reading YAML-parsed education array.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Tampering | EducationSection color classes | mitigate | Ripgrep acceptance criterion enforces zero raw color classes. `sm:text-right` and `mt-4` criteria guard layout invariants. |
</threat_model>

<verification>
- `rg "text-zinc-|border-zinc-|bg-zinc-|text-indigo-|bg-indigo-|outline-indigo-|bg-blue-|text-blue-|bg-white|text-white" src/components/EducationSection.tsx` → zero matches
- Token-presence greps as above
- `npm run lint` → exit 0
- (Plan 16-05 phase gate runs build + whole-directory grep + visual parity check)
</verification>

<success_criteria>
- TOKEN-01 partial (EducationSection scope) satisfied.
- Phase SC#1 partial (EducationSection scope) satisfied.
- Biome lint passes.
- Layout/typography invariant preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/16-token-unification/16-03-SUMMARY.md` documenting: lines changed, tokens applied, verification output.
</output>
