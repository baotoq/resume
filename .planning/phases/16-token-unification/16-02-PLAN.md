---
phase: 16-token-unification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/WorkExperience.tsx
autonomous: true
requirements:
  - TOKEN-01
tags:
  - tokens
  - refactor
  - visual-parity
  - timeline
must_haves:
  truths:
    - "WorkExperience.tsx contains zero raw text-zinc-*, border-zinc-*, bg-zinc-*, text-blue-*, bg-blue-*, bg-white classes"
    - "Timeline vertical line uses bg-border"
    - "Current-job dot uses bg-primary; inactive dot uses border-border bg-background"
    - "Role title uses text-primary; company name uses text-foreground; date range + duration use text-muted-foreground"
    - "Bullet <li> uses text-foreground with before:bg-border pseudo-element dot"
    - "Timeline geometry classes (absolute/relative/z-10/left offsets/top offsets/w-/h-) preserved"
  artifacts:
    - path: "src/components/WorkExperience.tsx"
      provides: "Color-token-driven experience timeline section"
      contains: "text-foreground"
  key_links: []
---

<objective>
Replace all raw zinc/blue/white color classes in `src/components/WorkExperience.tsx` with shadcn semantic tokens per 16-CONTEXT.md D-01 through D-06/D-08 and 16-UI-SPEC.md "Per-File Contracts — File 2". No structural or geometric changes. Nine lines touched (10 class-string replacements).

Purpose: Eliminate TOKEN-01 violations in the longest section component. Current-job dot adopts the phase-wide monochrome accent via `--primary`.
Output: `src/components/WorkExperience.tsx` with zero raw color classes, biome lint clean, timeline geometry untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-token-unification/16-CONTEXT.md
@.planning/phases/16-token-unification/16-UI-SPEC.md
@.planning/phases/16-token-unification/16-RESEARCH.md
@src/components/WorkExperience.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap raw color classes in WorkExperience.tsx to shadcn tokens (TOKEN-01)</name>
  <files>src/components/WorkExperience.tsx</files>
  <read_first>
    - src/components/WorkExperience.tsx (confirm current state — 9 lines to modify: 30, 38, 51, 52, 65, 72, 77, 80, 94)
    - src/app/globals.css (confirm :root tokens foreground, muted-foreground, border, primary, background exist)
    - .planning/phases/16-token-unification/16-UI-SPEC.md (File 2 contract table)
    - .planning/phases/16-token-unification/16-CONTEXT.md (D-01 through D-06, D-08)
  </read_first>
  <action>
Apply these exact substitutions. Only color-related class tokens change. All geometry, spacing, typography classes are preserved.

**Line 30** — section H2:
BEFORE:
```tsx
<h2 className="text-xl font-semibold leading-[1.2] text-zinc-900 mb-6">
```
AFTER:
```tsx
<h2 className="text-xl font-semibold leading-[1.2] text-foreground mb-6">
```

**Line 38** — vertical timeline line:
BEFORE:
```tsx
className="absolute left-0.75 sm:left-1.75 top-7 bottom-0 w-0.5 bg-zinc-200"
```
AFTER:
```tsx
className="absolute left-0.75 sm:left-1.75 top-7 bottom-0 w-0.5 bg-border"
```

**Lines 51-52** — timeline dot ternary (current vs inactive):
BEFORE:
```tsx
className={`absolute z-10 -left-5.5 sm:-left-6.5 top-5.5 w-3 h-3 rounded-full ${
  isCurrent
    ? "bg-blue-600"
    : "border-2 border-zinc-300 bg-white"
}`}
```
AFTER:
```tsx
className={`absolute z-10 -left-5.5 sm:-left-6.5 top-5.5 w-3 h-3 rounded-full ${
  isCurrent
    ? "bg-primary"
    : "border-2 border-border bg-background"
}`}
```

**Line 65** — role title H3:
BEFORE:
```tsx
<h3 className="font-bold text-blue-600 text-lg">
```
AFTER:
```tsx
<h3 className="font-bold text-primary text-lg">
```

**Line 72** — company name:
BEFORE:
```tsx
<p className="text-xl font-bold text-zinc-900">
```
AFTER:
```tsx
<p className="text-xl font-bold text-foreground">
```

**Line 77** — date range `<span>`:
BEFORE:
```tsx
<span className="text-sm font-bold text-zinc-500">
```
AFTER:
```tsx
<span className="text-sm font-bold text-muted-foreground">
```

**Line 80** — duration tag `<span>`:
BEFORE:
```tsx
<span className="text-xs text-zinc-400">
```
AFTER:
```tsx
<span className="text-xs text-muted-foreground">
```

**Line 94** — bullet `<li>` with pseudo-element dot:
BEFORE:
```tsx
className="text-base leading-relaxed text-zinc-700 pl-4 relative before:content-[''] before:absolute before:left-0 before:top-[10px] before:h-1.5 before:w-1.5 before:rounded-full before:bg-zinc-300"
```
AFTER:
```tsx
className="text-base leading-relaxed text-foreground pl-4 relative before:content-[''] before:absolute before:left-0 before:top-[10px] before:h-1.5 before:w-1.5 before:rounded-full before:bg-border"
```
(Two token swaps on same attribute: `text-zinc-700` → `text-foreground`; `before:bg-zinc-300` → `before:bg-border`.)

**Constraints:**
- Do NOT add any imports.
- Do NOT touch timeline geometry classes: `absolute left-0.75 sm:left-1.75 top-7 bottom-0 w-0.5`, `absolute z-10 -left-5.5 sm:-left-6.5 top-5.5 w-3 h-3 rounded-full`, `before:absolute before:left-0 before:top-[10px] before:h-1.5 before:w-1.5 before:rounded-full`.
- Do NOT touch `border-2` on the inactive dot (border width, not color — preserve).
- Do NOT touch `mb-6`, `text-xl`, `font-bold`, `font-semibold`, `text-lg`, `text-base`, `text-sm`, `text-xs`, `leading-relaxed`, `leading-[1.2]`, `pl-4`, `relative`, `before:content-['']`, `before:rounded-full`.
- Do NOT touch the `isCurrent` ternary structure.
- Do NOT change JSX (no new tags, no reordering, no prop renames).
- Do NOT introduce `cn()`.

After edits:
```bash
npm run format
```
  </action>
  <verify>
    <automated>test $(rg -c "text-zinc-|border-zinc-|bg-zinc-|text-indigo-|bg-indigo-|outline-indigo-|bg-blue-|text-blue-|bg-white|text-white" src/components/WorkExperience.tsx || echo 0) -eq 0 && test $(grep -c "text-foreground" src/components/WorkExperience.tsx) -ge 3 && test $(grep -c "text-muted-foreground" src/components/WorkExperience.tsx) -ge 2 && test $(grep -c "text-primary" src/components/WorkExperience.tsx) -ge 1 && test $(grep -c "bg-primary" src/components/WorkExperience.tsx) -ge 1 && test $(grep -c "border-border" src/components/WorkExperience.tsx) -ge 1 && test $(grep -c "bg-background" src/components/WorkExperience.tsx) -ge 1 && test $(grep -c "bg-border" src/components/WorkExperience.tsx) -ge 2 && test $(grep -c "before:rounded-full" src/components/WorkExperience.tsx) -eq 1 && test $(grep -c "top-5.5 w-3 h-3 rounded-full" src/components/WorkExperience.tsx) -eq 1 && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "text-zinc-|border-zinc-|bg-zinc-|text-indigo-|bg-indigo-|outline-indigo-|bg-blue-|text-blue-|bg-white|text-white" src/components/WorkExperience.tsx` returns 0 / no matches
    - `grep -c "text-foreground" src/components/WorkExperience.tsx` returns `>= 3` (H2 + company name + bullets)
    - `grep -c "text-muted-foreground" src/components/WorkExperience.tsx` returns `>= 2` (date range + duration)
    - `grep -c "text-primary" src/components/WorkExperience.tsx` returns `>= 1` (role title H3)
    - `grep -c "bg-primary" src/components/WorkExperience.tsx` returns `>= 1` (current-job dot)
    - `grep -c "border-border" src/components/WorkExperience.tsx` returns `>= 1` (inactive dot border)
    - `grep -c "bg-background" src/components/WorkExperience.tsx` returns `>= 1` (inactive dot fill)
    - `grep -c "bg-border" src/components/WorkExperience.tsx` returns `>= 2` (vertical line + bullet pseudo-dot)
    - `grep -c "before:rounded-full" src/components/WorkExperience.tsx` returns `1` (bullet pseudo-dot shape preserved)
    - `grep -c "top-5.5 w-3 h-3 rounded-full" src/components/WorkExperience.tsx` returns `1` (timeline dot geometry preserved)
    - `grep -c "isCurrent" src/components/WorkExperience.tsx` returns `>= 1` (ternary preserved)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>
    WorkExperience.tsx uses only shadcn semantic tokens for color. Timeline geometry, ternary structure, typography, spacing unchanged. Current-job dot now near-black via `--primary`. Biome clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. Server Component reading YAML-parsed experience array. No user input.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Tampering | WorkExperience color classes | mitigate | Ripgrep acceptance criterion enforces zero raw color classes. |
| T-16-04 | Tampering | Timeline geometry | mitigate | Acceptance criteria on geometry class strings (`top-5.5 w-3 h-3 rounded-full`, `before:rounded-full`, `isCurrent` ternary) guard against structural regression. |
</threat_model>

<verification>
- `rg "text-zinc-|border-zinc-|bg-zinc-|text-indigo-|bg-indigo-|outline-indigo-|bg-blue-|text-blue-|bg-white|text-white" src/components/WorkExperience.tsx` → zero matches
- 10 token-presence grep checks (see acceptance_criteria)
- `npm run lint` → exit 0
- (Plan 16-05 phase gate runs build + whole-directory grep + visual parity check)
</verification>

<success_criteria>
- TOKEN-01 partial (WorkExperience scope) satisfied.
- Phase SC#1 partial (WorkExperience scope) satisfied.
- Phase SC#2 partial satisfied (`bg-white` in WorkExperience replaced with `bg-background`).
- Biome lint passes.
- Timeline geometry + ternary structure unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/16-token-unification/16-02-SUMMARY.md` documenting: lines changed, tokens applied, any surprises, verification output.
</output>
