---
phase: 13-shadcn-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/components/ui/card.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/separator.tsx
autonomous: false
requirements:
  - SHAD-02

must_haves:
  truths:
    - "card.tsx, badge.tsx, and separator.tsx exist in src/components/ui/ as CLI-generated source files"
    - "All 3 component files pass Biome lint with no violations"
    - "npm run build passes with zero TypeScript errors after adding components"
    - "No section components (Header, WorkExperience, EducationSection) were modified"
    - "Page is visually identical before and after Phase 13 — no rendered elements changed"
  artifacts:
    - path: "src/components/ui/card.tsx"
      provides: "Card, CardHeader, CardContent, CardFooter, CardTitle, CardDescription primitives"
      exports: ["Card", "CardHeader", "CardContent", "CardFooter", "CardTitle", "CardDescription"]
    - path: "src/components/ui/badge.tsx"
      provides: "Badge component with variant support"
      exports: ["Badge", "badgeVariants"]
    - path: "src/components/ui/separator.tsx"
      provides: "Separator horizontal/vertical divider (Radix-backed)"
      exports: ["Separator"]
      contains: '"use client"'
  key_links:
    - from: "src/components/ui/card.tsx"
      to: "src/lib/utils.ts"
      via: 'import { cn } from "@/lib/utils"'
      pattern: 'cn.*@/lib/utils'
    - from: "src/components/ui/badge.tsx"
      to: "node_modules/class-variance-authority"
      via: "import { cva } from \"class-variance-authority\""
      pattern: "class-variance-authority"
    - from: "src/components/ui/separator.tsx"
      to: "node_modules/@radix-ui/react-separator"
      via: 'import * as SeparatorPrimitive from "@radix-ui/react-separator"'
      pattern: "@radix-ui/react-separator"
---

<objective>
Run `npx shadcn@latest add card badge separator` to install the three component source files into
src/components/ui/, then fix any Biome lint violations in the generated files, run the final
build + lint gate, and verify zero visual change in browser.

Purpose: Complete the SHAD-02 requirement — shadcn component sources present and TypeScript-clean.
This plan requires Plan 01 to have completed (components.json + utils.ts must exist before the
shadcn CLI can write component files).

Output:
- src/components/ui/card.tsx
- src/components/ui/badge.tsx
- src/components/ui/separator.tsx
- All three files Biome-clean and TypeScript-clean
</objective>

<execution_context>
@/Users/baotoquoc-covergo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoquoc-covergo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/baotoquoc-covergo/Work/resume/.planning/PROJECT.md
@/Users/baotoquoc-covergo/Work/resume/.planning/ROADMAP.md
@/Users/baotoquoc-covergo/Work/resume/.planning/STATE.md
@/Users/baotoquoc-covergo/Work/resume/.planning/phases/13-shadcn-infrastructure/13-CONTEXT.md
@/Users/baotoquoc-covergo/Work/resume/.planning/phases/13-shadcn-infrastructure/13-RESEARCH.md
@/Users/baotoquoc-covergo/Work/resume/.planning/phases/13-shadcn-infrastructure/13-UI-SPEC.md
@/Users/baotoquoc-covergo/Work/resume/.planning/phases/13-shadcn-infrastructure/13-01-SUMMARY.md

<interfaces>
<!-- Plan 01 outputs this plan depends on: -->

components.json at project root — required by shadcn CLI:
- "style": "new-york"
- "aliases": { "ui": "@/components/ui", "utils": "@/lib/utils" }
- "rsc": true

src/lib/utils.ts — imported by CLI-generated component files:
```typescript
import { clsx, type ClassValue } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

<!-- Expected component file structure (from PATTERNS.md): -->

card.tsx expected sub-components: Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter
badge.tsx expected exports: badgeVariants (cva-based), Badge (accepts variant prop)
separator.tsx expected: "use client" directive, @radix-ui/react-separator dependency

<!-- Biome suppression pattern (from PATTERNS.md shared patterns): -->
```tsx
/** biome-ignore-all lint/suspicious/noArrayIndexKey: <explanation> */
```
Use biome-ignore with explanation string when generated code cannot be safely changed without
breaking component behavior.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install component sources and fix Biome violations</name>
  <files>src/components/ui/card.tsx, src/components/ui/badge.tsx, src/components/ui/separator.tsx</files>
  <read_first>
    - /Users/baotoquoc-covergo/Work/resume/components.json (confirm it exists and has "style": "new-york" before running CLI)
    - /Users/baotoquoc-covergo/Work/resume/src/lib/utils.ts (confirm it exists before running CLI — CLI-generated files import from @/lib/utils)
    - /Users/baotoquoc-covergo/Work/resume/.planning/phases/13-shadcn-infrastructure/13-RESEARCH.md (Pitfall 5 — Biome lint on shadcn-generated files — read before fixing violations)
    - /Users/baotoquoc-covergo/Work/resume/.planning/phases/13-shadcn-infrastructure/13-PATTERNS.md (Biome suppression pattern + expected generated structure for card/badge/separator)
  </read_first>
  <action>
**Step 1: Run the batch install command**

```bash
cd /Users/baotoquoc-covergo/Work/resume && npx shadcn@latest add card badge separator
```

If prompted "Would you like to install dependencies?" — answer yes.
If prompted about overwriting any file other than the ui/ components — answer no and investigate before proceeding.
Do NOT accept any prompt to overwrite globals.css.

This creates:
- src/components/ui/card.tsx
- src/components/ui/badge.tsx
- src/components/ui/separator.tsx
- Also auto-installs @radix-ui/react-separator (required by separator.tsx)

**Step 2: Run lint immediately after install**

```bash
cd /Users/baotoquoc-covergo/Work/resume && npm run lint
```

**Step 3: Fix Biome violations**

If lint exits non-zero, run the auto-fix first:
```bash
cd /Users/baotoquoc-covergo/Work/resume && npx biome check --write src/components/ui/
```

Common violations and how to fix them (per RESEARCH.md Pitfall 5 + PATTERNS.md):
- Import ordering: auto-fixable by biome check --write
- `import { type X }` vs `import type { X }`: both are accepted in Biome 2.2.0; do not force change
- Unfixable patterns (CVA type generics, Radix spread patterns): add `/** biome-ignore lint/<rule>: generated by shadcn CLI */` above the flagged line
- Do NOT alter component logic — only fix formatting/import-order/suppress-with-comment issues

**Step 4: Verify all 3 component files exist**

```bash
ls /Users/baotoquoc-covergo/Work/resume/src/components/ui/
```

Expected output: badge.tsx  card.tsx  separator.tsx

**Step 5: Run lint again to confirm clean**

```bash
cd /Users/baotoquoc-covergo/Work/resume && npm run lint
```

Must exit 0 before proceeding.

**Do NOT modify**: Header.tsx, WorkExperience.tsx, EducationSection.tsx, TechStackIcons.tsx,
layout.tsx, globals.css (already merged in Plan 01), tsconfig.json, next.config.ts.
  </action>
  <verify>
    <automated>ls /Users/baotoquoc-covergo/Work/resume/src/components/ui/ && cd /Users/baotoquoc-covergo/Work/resume && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/card.tsx exists
    - src/components/ui/badge.tsx exists
    - src/components/ui/separator.tsx exists
    - grep '"use client"' src/components/ui/separator.tsx returns 1 match (Radix component requires client directive)
    - grep 'cn.*@/lib/utils' src/components/ui/card.tsx returns a match (imports cn utility)
    - grep 'class-variance-authority' src/components/ui/badge.tsx returns a match (CVA dependency)
    - grep '@radix-ui/react-separator' src/components/ui/separator.tsx returns a match (Radix dependency)
    - npm run lint exits 0 (no Biome violations)
  </acceptance_criteria>
  <done>card.tsx, badge.tsx, separator.tsx exist in src/components/ui/. All three pass Biome lint with exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Run final build + lint gate (D-11)</name>
  <files></files>
  <read_first>
    - /Users/baotoquoc-covergo/Work/resume/.planning/phases/13-shadcn-infrastructure/13-CONTEXT.md (D-11 success gate — review the complete list of conditions before running)
  </read_first>
  <action>
Run the D-11 success gate checks in order:

**Check 1: Full TypeScript build**
```bash
cd /Users/baotoquoc-covergo/Work/resume && npm run build
```
Must exit 0. If TypeScript errors appear in src/components/ui/*.tsx files, fix the type error
(likely an unused import or missing type annotation from Biome auto-fix). Do not silence errors
with @ts-ignore — fix the root cause.

**Check 2: Lint clean across all files**
```bash
cd /Users/baotoquoc-covergo/Work/resume && npm run lint
```
Must exit 0. If violations remain after Task 1 auto-fix, suppress with biome-ignore comments.

**Check 3: Verify artifact existence**
```bash
test -f /Users/baotoquoc-covergo/Work/resume/src/lib/utils.ts && echo "utils.ts OK"
test -f /Users/baotoquoc-covergo/Work/resume/components.json && echo "components.json OK"
test -f /Users/baotoquoc-covergo/Work/resume/src/components/ui/card.tsx && echo "card.tsx OK"
test -f /Users/baotoquoc-covergo/Work/resume/src/components/ui/badge.tsx && echo "badge.tsx OK"
test -f /Users/baotoquoc-covergo/Work/resume/src/components/ui/separator.tsx && echo "separator.tsx OK"
```
All five must print OK.

**Check 4: Confirm no section components were modified**
```bash
git diff --name-only HEAD -- src/components/Header.tsx src/components/WorkExperience.tsx src/components/EducationSection.tsx src/app/layout.tsx
```
Must return empty output (no changes to these files).
  </action>
  <verify>
    <automated>cd /Users/baotoquoc-covergo/Work/resume && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0, zero TypeScript errors
    - npm run lint exits 0, zero Biome violations
    - src/lib/utils.ts, components.json, card.tsx, badge.tsx, separator.tsx all exist
    - git diff on Header.tsx, WorkExperience.tsx, EducationSection.tsx, layout.tsx returns empty (no modifications)
  </acceptance_criteria>
  <done>D-11 gate passed: build clean, lint clean, all 5 required artifacts exist, no section components modified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01 merged globals.css (shadcn tokens + Geist font vars, no hex values).
    Plan 02 Task 1 installed card.tsx, badge.tsx, separator.tsx via shadcn CLI and fixed Biome violations.
    Plan 02 Task 2 confirmed npm run build and npm run lint both pass.

    Zero user-facing elements were modified in Phase 13. The preservation contract requires the
    page to look IDENTICAL to its pre-phase state.
  </what-built>
  <how-to-verify>
    1. Run: `npm run dev` (from /Users/baotoquoc-covergo/Work/resume)
    2. Open http://localhost:3000 in browser
    3. Verify ALL of the following are unchanged from before Phase 13:
       - Page background is off-white zinc-50 (not pure white, not grey)
       - Body text is near-black zinc-900 (not visibly lighter or darker)
       - Body font is Geist Sans (not system-ui/sans-serif fallback — check DevTools computed font)
       - Header card has the same rounded border with white background
       - Work experience cards look identical
       - Education section cards look identical
       - Tech stack icon pills are unchanged
       - No new visual elements appear (no separator lines between sections, no badges changed)
    4. Check browser DevTools → Elements → computed CSS on body:
       - `font-family` should start with Geist (not "system-ui" or "-apple-system")
       - `background-color` should be approximately rgb(250,250,250) — zinc-50
  </how-to-verify>
  <resume-signal>Type "approved" if page looks identical to before Phase 13, or describe any visual difference observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — This plan installs component source files from the official shadcn registry and runs local
build/lint tooling. No authentication, user input, or runtime data processing is introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Tampering | npx shadcn@latest add (registry download) | accept | Components sourced from official ui.shadcn.com registry; open-source; no network-access or eval patterns in Card/Badge/Separator source (per UI-SPEC Registry Safety section) |

</threat_model>

<verification>
Full Phase 13 verification (D-11 gate):

1. `npm run build` exits 0 — zero TypeScript errors across all files including new ui/ components
2. `npm run lint` exits 0 — zero Biome violations across all files
3. src/lib/utils.ts exists with `export function cn`
4. components.json exists at root with `"style": "new-york"` and `"rsc": true`
5. src/components/ui/card.tsx, badge.tsx, separator.tsx all present
6. globals.css contains `@import "tw-animate-css"`, `@import "shadcn/tailwind.css"`, `--font-geist-sans`, no hex values
7. Human-verified: browser shows zero visual change from pre-Phase-13 state
</verification>

<success_criteria>
- card.tsx, badge.tsx, separator.tsx exist in src/components/ui/ (SHAD-02 complete)
- separator.tsx contains "use client" directive
- npm run build exits 0
- npm run lint exits 0
- Human verification: page visually identical before and after phase
</success_criteria>

<output>
After completion, create `.planning/phases/13-shadcn-infrastructure/13-02-SUMMARY.md`
</output>
