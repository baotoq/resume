---
phase: 15-badge-and-separator
plan: 03
type: execute
wave: 2
depends_on:
  - 15-01
  - 15-02
files_modified: []
autonomous: false
requirements:
  - BADGE-01
  - SEP-01
tags:
  - shadcn
  - badge
  - separator
  - gate
  - visual-parity
must_haves:
  truths:
    - "`npm run build` exits 0 after both primitive swaps"
    - "`npm run lint` exits 0 after both primitive swaps"
    - "Only two files were modified (TechStackIcons.tsx, page.tsx) — Header.tsx untouched (SC#4)"
    - "Fallback Badge visible with readable contrast at 375px and 1280px on live render"
    - "Two horizontal Separator lines visible between resume sections at 375px and 1280px"
    - "No animation jank — Separators fade in coupled with following card (D-05)"
  artifacts:
    - path: ".planning/phases/15-badge-and-separator/15-03-SUMMARY.md"
      provides: "Phase gate verdict + visual confirmation notes"
  key_links:
    - from: "executor"
      to: "user (visual verifier)"
      via: "checkpoint:human-verify"
      pattern: "npm run dev"
---

<objective>
Phase 15 exit gate. Plans 15-01 and 15-02 each made one primitive swap in wave 1 with per-file grep + lint verification. This plan (wave 2) runs the full `npm run build && npm run lint` across the integrated result, confirms scope lock (Header.tsx untouched), and pauses for the user to confirm visual delivery at 375px and 1280px.

Purpose: Prove BADGE-01 and SEP-01 are satisfied together, build still compiles, Header's inline `·` dots were not regressed, and the live-render result matches the roadmap success criteria.
Output: Gate verdict in 15-03-SUMMARY.md; either GREEN (phase passes) or RED with named regressions (drives gap-closure replan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-badge-and-separator/15-CONTEXT.md
@.planning/phases/15-badge-and-separator/15-UI-SPEC.md
@.planning/phases/15-badge-and-separator/15-VALIDATION.md
@.planning/phases/15-badge-and-separator/15-01-SUMMARY.md
@.planning/phases/15-badge-and-separator/15-02-SUMMARY.md
@src/components/techstack-icons/TechStackIcons.tsx
@src/app/page.tsx
@src/components/Header.tsx
@src/components/ui/badge.tsx
@src/components/ui/separator.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-file grep gate + full build + lint (BADGE-01 + SEP-01)</name>
  <files></files>
  <read_first>
    - .planning/phases/15-badge-and-separator/15-UI-SPEC.md ("Verification Anchors" section — exact grep commands)
    - .planning/phases/15-badge-and-separator/15-VALIDATION.md (Per-Task Verification Map row 15-03-01)
    - .planning/phases/15-badge-and-separator/15-CONTEXT.md (D-07 scope lock, D-10 verification method)
    - .planning/phases/15-badge-and-separator/15-01-SUMMARY.md (plan 01 outcome)
    - .planning/phases/15-badge-and-separator/15-02-SUMMARY.md (plan 02 outcome)
  </read_first>
  <action>
Run the full integrated verification suite. Execute each command and capture output. Do NOT modify any source files in this task.

**Step 1 — Cross-file grep for BADGE-01 and SEP-01 (integrated verification):**

```bash
# BADGE-01 positive — Badge applied in fallback branch
grep -n '<Badge variant="secondary">' src/components/techstack-icons/TechStackIcons.tsx

# BADGE-01 negative — hand-rolled pill gone
grep -n 'bg-zinc-100 text-zinc-600 rounded-full' src/components/techstack-icons/TechStackIcons.tsx

# SEP-01 positive — two separators
grep -c '<Separator />' src/app/page.tsx

# SEP-01 import present
grep -n 'from "@/components/ui/separator"' src/app/page.tsx

# Animation coupling (D-05)
grep -c '<AnimateIn delay={0.1}>' src/app/page.tsx
grep -c '<AnimateIn delay={0.2}>' src/app/page.tsx
```

Expected: first grep ≥ 1 match, second grep 0 matches (exit 1), third grep = 2, fourth grep 1 match, fifth and sixth grep each = 2.

**Step 2 — Scope lock: Header.tsx must NOT have been modified in this phase.**

```bash
# Check the last 3 commits for Header.tsx changes (plans 15-01 and 15-02 only touch other files)
git log --name-only --pretty=format: HEAD~3..HEAD | grep -c 'src/components/Header.tsx' || true
```

Expected: `0` (Header.tsx not in any plan's diff).

**Step 3 — Full build + lint:**

```bash
npm run lint
npm run build
```

Both must exit 0.

**Step 4 — Checkpoint: human visual verification.**

This task has `autonomous: false` — stop here and request the user run `npm run dev`, open the resume at 375px mobile and 1280px desktop, and confirm:

1. **Fallback Badge (BADGE-01 / SC#1):** Any unrecognized tech-stack string (one not in `TECH_ICON_MAP`) renders as a neutral rounded pill with readable contrast on the white Card background. If the current resume.md has no fallback cases, add a temporary entry with an unknown tech name (e.g., `"not-a-real-tech"`) to confirm, then revert.
2. **Icon rows unchanged (SC#2):** Recognized tech stacks still render as SVG icons with hover tooltips — identical to pre-Phase-15 appearance.
3. **Two horizontal separators (SEP-01 / SC#3):** Exactly two 1px horizontal lines visible — one between Header card and WorkExperience card, one between WorkExperience card and EducationSection card. Line color matches card border hue.
4. **Header contact dots (SC#4):** The `·` dots between Email / Phone / GitHub / LinkedIn in the Header are still plain text — flowing inline with links, not replaced by block elements.
5. **Animation coherence (D-05):** On page refresh, each Separator fades in as a coordinated unit with the card below it — not visible in empty space before the card enters.

If any check fails → record RED verdict with specific regression → phase goes to gap-closure replan.
If all checks pass → record GREEN verdict → phase 15 complete.
  </action>
  <verify>
    <automated>test $(grep -c '<Badge variant="secondary">' src/components/techstack-icons/TechStackIcons.tsx) -ge 1 && test $(grep -c 'bg-zinc-100 text-zinc-600 rounded-full' src/components/techstack-icons/TechStackIcons.tsx) -eq 0 && test $(grep -c '<Separator />' src/app/page.tsx) -eq 2 && test $(grep -c 'from "@/components/ui/separator"' src/app/page.tsx) -eq 1 && test $(grep -c '<AnimateIn delay={0.1}>' src/app/page.tsx) -eq 2 && test $(grep -c '<AnimateIn delay={0.2}>' src/app/page.tsx) -eq 2 && npm run lint && npm run build</automated>
    <manual>User confirms at 375px and 1280px: fallback Badge readable, icon rows unchanged, two horizontal separators visible, Header `·` dots are text, Separators animate coupled with following cards.</manual>
  </verify>
  <acceptance_criteria>
    - `grep -c '<Badge variant="secondary">' src/components/techstack-icons/TechStackIcons.tsx` returns `>= 1`
    - `grep -c 'bg-zinc-100 text-zinc-600 rounded-full' src/components/techstack-icons/TechStackIcons.tsx` returns `0`
    - `grep -c '<Separator />' src/app/page.tsx` returns `2`
    - `grep -c 'from "@/components/ui/separator"' src/app/page.tsx` returns `1`
    - `grep -c '<AnimateIn delay={0.1}>' src/app/page.tsx` returns `2`
    - `grep -c '<AnimateIn delay={0.2}>' src/app/page.tsx` returns `2`
    - `grep -c '<AnimateIn delay={0}>' src/app/page.tsx` returns `1`
    - `git log --name-only --pretty=format: HEAD~3..HEAD | grep -c 'src/components/Header.tsx'` returns `0` (Header untouched by Phase 15)
    - `npm run lint` exits 0
    - `npm run build` exits 0
    - User manually confirms all 5 visual-gate items (recorded in 15-03-SUMMARY.md)
  </acceptance_criteria>
  <done>
    All grep gates pass, build and lint green, Header.tsx scope-lock confirmed, user has recorded GREEN verdict on visual parity check in 15-03-SUMMARY.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. This plan runs verification commands and reads files — does not modify source.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08 | Tampering | Gate false-green (lint passes but visual broken) | mitigate | `autonomous: false` forces human checkpoint — automated greps cannot catch contrast regression or animation jank. D-10 formalizes manual verification as the only source of truth for visual parity. |
| T-15-09 | Tampering | Header.tsx collateral modification | mitigate | Acceptance criterion `git log --name-only HEAD~3..HEAD | grep -c 'Header.tsx' = 0` — enforces SC#4 scope lock across the full phase commit range. |
| T-15-10 | Denial of Service | npm run build timeout on Next.js 16 | accept | Build gate was proven in Phase 14-04 at ~23s. No Next.js config changes this phase. |
</threat_model>

<verification>
- Full cross-file grep suite (Step 1) — all counts match acceptance criteria.
- `git log --name-only --pretty=format: HEAD~3..HEAD | grep -c 'src/components/Header.tsx'` → 0.
- `npm run lint` → exit 0.
- `npm run build` → exit 0.
- User-confirmed visual check at 375px and 1280px — Badge readable, separators visible, Header dots preserved, animation coherent.
</verification>

<success_criteria>
- BADGE-01 verified integrated: fallback Badge live in TechStackIcons, no regressions.
- SEP-01 verified integrated: two horizontal Separators live in page.tsx, animation-coupled.
- SC#1, SC#2, SC#3, SC#4, SC#5 all satisfied simultaneously on live render.
- Phase 15 GREEN gate → ready for `/gsd-verify-work` or next phase.
</success_criteria>

<output>
After completion, create `.planning/phases/15-badge-and-separator/15-03-SUMMARY.md` documenting:
- Full grep output for Step 1 (all six queries)
- `git log` scope-lock check result
- `npm run lint` and `npm run build` exit status + timing
- User's visual-check verdict for each of the 5 manual items (pass/fail per item)
- Final verdict: GREEN or RED (with named regressions if RED)
</output>
